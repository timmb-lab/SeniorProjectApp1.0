# Demo Role Readiness

This scorecard records what is proven for demo roles and what still needs hosted click-through. It should be updated from tests, local proof, and safe hosted proof only.

## 1. Run Snapshot

- Last updated: `2026-06-03T03:39:08-07:00`
- Starting branch/SHA: `main` at `2975050c9472ddf5e0668feb1206a46c7a6ee1b1`
- Ending branch/SHA: pending closeout commit; final SHA is reported after commit and push.
- Baseline push first: no. `main` started clean but ahead of `origin/main` by eleven earlier in-lane commits, and this run stayed local-only without touching remotes.
- Hosted click-through this run: not completed; this was a local Program Teacher mentor-meeting follow-up handoff pass with focused route and workspace coverage.

## 4. Current Role Readiness Table

| Role | Current evidence | Remaining gap |
| --- | --- | --- |
| Global Admin | Local proof continues to allow Global Admin on `/api/site/programs`, including opening another demo site. Global Admin still sees the broader admin and site surfaces already covered by existing local route proof, the topbar `Find a student` search reuses the existing scoped Student Directory instead of inventing a second search path, the Site Dashboard `Recent Activity` summary now exposes a real `Open audit` shortcut that lands in the existing Audit section when that route is available, `Users & Access` now shows a disclosed recent-role-assignments panel from `/api/admin/role-assignments` with readable site, program, and cohort labels, assigner names, a real `Open school access` handoff for school-scoped grants, exact `Open program students` or `Open cohort students` handoffs when those grants map to one accessible school, and explicit school-choice buttons when multi-school program or cohort grants map to more than one accessible school, plus explicit site-pick guidance when `/api/site/access-assignments` requires choosing a school before access management, the Review Queue now shows explicit school-pick guidance when `/api/site/review-queue` requires choosing a school before loading submitted work, the Admin Command Center now exposes a direct `Programs` quick action while the Programs section shows school-pick guidance when `/api/site/programs` requires selecting a school before changing program mappings, the Admin Command Center `Needs Attention` card opens the existing missing-mentor student list, revision queue, mentor-meeting follow-up student list, presentation outline follow-up schedule, and failed export follow-up list, the `Review Workload` card now opens the existing student-detail drawer from submitted or revision rows, that detail now reloads or closes back inside the Admin Command Center URL instead of jumping to the broader Student Directory, the same drawer now renders explicit school-pick guidance when `/api/site/students/:studentId` requires a current school before protected detail can load, and the `Recent Audit` card now opens the existing Audit section with exact `action` and `entityType` filters from each recent row. | Needs a broader role refresh that checks global-versus-site boundaries, cross-site Programs behavior, filtered Audit behavior across schools, and the access-management and Review Queue site-selection handoffs. |
| Site Admin | Site Admin Programs still has local first-load add proof. The Site Dashboard and Users & Access first load are now calmer: dashboard snapshots, access history, and long guidance are disclosed while route-backed metrics and current access summary stay visible. Site Dashboard follow-up and protected-access summaries now use school-facing `Teacher follow-up` and protected-and-reviewed language, the top-risk list now explains why each student is highlighted, the `Needs Attention` rows now open the existing missing-mentor Student Directory filter plus the existing revision-requested and presentation/archive Operations worklists, the `Next Actions` card now opens the all-students list, the review queue, and archive/presentation follow-up through existing presets, the disclosed `Recent Activity` card now lists recent submissions, evidence, and teacher feedback without sensitive private details, summary-strip counts that still stay passive now render explicit `Summary only` treatment, Operations uses school follow-up language instead of viewer-style read-only monitoring copy, Mentor Assignments route responses now use school-facing coverage and follow-up wording, the mentor-assignment POST route now keeps working for multi-site site admins when the current school is implied instead of explicitly posted, the Review Queue now shows explicit school-pick guidance when `/api/site/review-queue` requires choosing a school before review work can load, student detail can now reload or share the selected record, tab, and timeline inside the current scoped section URL, the topbar `Find a student` search now opens the existing Student Directory with the current school scope from any workspace section, student-detail Reviews now label student-visible versus staff-only notes when the route provides that boundary, and the Presentation section now restores the current ready-for-check-out, checked-out, checked-in, or outline-follow-up focus from the same scoped section URL. | Hosted click-through remains pending. |
| Administration | Local proof confirms Administration can use read-only monitoring routes where already allowed and is denied from `/api/site/programs`; the workspace nav keeps Programs hidden, Site Dashboard now uses leadership monitoring labels instead of viewer wording, site-backed Readiness now uses leadership-readiness copy, Operations keeps explicit read-only monitoring language, the `Needs Attention` card keeps only the role-safe Student Directory and Operations drill-downs visible instead of exposing Review Queue actions, the disclosed `Next Actions` card now routes follow-up only through existing Student Directory and Operations presets while unavailable Review Queue destinations stay summary-only, the disclosed `Recent Activity` card now shows redacted recent student/workflow updates without opening a Global Admin audit path, read-only student detail links now restore the current record inside the same scoped section URL, the topbar `Find a student` search now reuses the same read-only Student Directory scope from any section, student-detail Reviews now explain role-scoped note visibility without exposing admin-only context, and the read-only Presentation section now restores the current day-of focus from the same scoped section URL. | Needs hosted click-through across Site Dashboard, Students, Operations, Presentation, and Readiness. |
| Program Teacher | Local proof keeps Program Teacher denied from `/api/site/programs` while preserving existing scoped dashboard, directory, review, and operations behavior. The Program Dashboard first load now keeps route-backed metrics visible and moves repeated detail panels behind dashboard disclosure, its `Needs Attention` rows now open the existing Student Directory, Review Queue, and Operations presets for missing mentor coverage, mentor-meeting follow-up, submitted work, missing evidence, behind-support students, revision follow-up, and presentation readiness, the new mentor-meeting handoff reuses the scoped Student Directory with an exact `mentor_meeting_follow_up` filter backed by each visible student's latest mentor meeting status, the Review Queue now shows explicit school-pick guidance when `/api/site/review-queue` requires choosing a school before scoped review work can load, Operations frames follow-up as assigned-student program work, the Review Queue distinguishes submitted `Open review` rows from revision-only follow-up rows while explaining active risk reasons, selected student detail can reload or share safely inside the current queue or program-dashboard URL, the topbar `Find a student` search now opens the same program-scoped Student Directory search from any workspace section, student-detail Reviews now explain when notes are only visible inside the current scoped detail, and the Presentation section now restores the current ready-for-check-out, checked-out, checked-in, or outline-follow-up focus from the same scoped section URL. | Needs hosted click-through; mentor assignment and broader off-dashboard workflow expansion remain intentionally out of scope for this role. |
| Mentor | Local proof keeps Mentor denied from `/api/site/programs` while preserving assigned-student mentor dashboard paths. Mentor Dashboard rows now render as compact assigned-student summaries with secondary meeting/presentation/activity details disclosed per row, the top Assigned/Needs Revision/Meetings/Presentations metrics now reuse the existing mentor focus filters instead of staying summary-only, mentor-opened student detail can now reload or share from the current mentor-dashboard focus URL without broadening access, student-detail Reviews now keep note labels role-scoped instead of exposing raw staff-only wording to mentor readers, and the mentor-visible Presentation section now restores the current day-of focus from the same scoped section URL. | Needs hosted/live proof and a broader mentor-support workflow review. |
| Viewer | Local proof keeps Viewer denied from `/api/site/programs`; workspace navigation keeps Programs hidden, the default monitoring path uses supported Student Directory filters, the new topbar `Find a student` search reuses that same read-only Student Directory scope, read-only student detail can now reload or share from the same scoped directory URL, student-detail Reviews now explain that the visible notes stay within the current read-only detail scope while admin-only context stays hidden, and denied protected sections now use explicit access-limited wording instead of `unavailable` route copy. | Needs hosted click-through for the read-only landing, assigned student list, and detail paths. |
| Student | Local proof keeps Student denied from `/api/site/programs` while preserving own-student dashboard coverage. Student first load now prioritizes progress, one current action, short next steps, and deadlines, with requirements, feedback, progress/support, evidence, submitted work, and files behind disclosure, and the Presentation section now restores the student's current schedule focus from the same scoped section URL. | Needs a full student refresh for hosted requirement drill-downs, evidence states, feedback flows, and archive/presentation visibility. |

## 6. Role-By-Role Loaded API Routes

- Global Admin: locally proven on `/api/site/programs` and existing admin/site route surfaces; the topbar `Find a student` search now routes back through the existing scoped `/api/site/students` path instead of a parallel search surface, the Site Dashboard `Recent Activity` summary now opens the existing Audit section through a real local `data-section="audit"` action while the detailed activity list stays redacted, `Users & Access` now loads `/api/admin/role-assignments` into a read-only recent-role-assignments disclosure with route-backed site, program, and cohort labels plus assigner names, school-scoped rows can switch directly into the matching current-school access-management context through the existing site-switch contract, single-school program-scoped or cohort-scoped rows can switch directly into the existing filtered Student Directory through the same protected panel, and multi-school program-scoped or cohort-scoped rows now render explicit school-choice buttons for each exact mapped accessible school while still showing explicit site-pick guidance if `/api/site/access-assignments` returns `site_selection_required`, the Review Queue now shows explicit school-pick guidance if `/api/site/review-queue` returns `site_selection_required`, the Admin Command Center detail panels now expose a `Programs` quick action while the Programs section shows explicit school-pick guidance if `/api/site/programs` returns `site_selection_required`, the Admin Command Center `Needs Attention` rows now reuse the existing `students`, `teacher`, `presentation`, and `archiveExports` filters where exact destinations already exist, including mentor-meeting follow-up through the current Student Directory `mentor_meeting_follow_up` preset, the `Review Workload` detail card now opens the existing `/api/site/students/:studentId` drawer and preserves `section=adminDashboard` detail URL state, that same drawer now turns `/api/site/students/:studentId` `site_selection_required` into school-pick guidance instead of a generic protected-detail error, the `Recent Audit` card now reuses `/api/admin/audit-events?action=...&entityType=...` to load exact filtered protected activity inside the existing Audit section, and the `Archive / Exports` section now lists real current package requests through failed, in-progress, and complete follow-up filters.
- Site Admin: locally proven on `/api/site/programs`, including first-load add, remove, restore, and active-program restore behavior, plus `/api/site/mentor-assignments` POST fallback when a multi-site site admin omits `siteId`; current local dashboard evidence also keeps follow-up and protected-access copy school-facing, keeps Mentor Assignments response copy school-facing, explains top-risk rows directly, opens `Needs Attention` rows through the existing Students, Review Queue, and Operations filters, opens the disclosed `Next Actions` card through the existing Students, Review Queue, and Operations presets, shows a redacted in-dashboard `Recent Activity` list backed by the site dashboard route, renders explicit summary-only treatment for passive summary-strip counts that do not have a backed route, restores student detail from existing section URLs without bypassing scoped route checks, exposes a topbar `Find a student` search that reuses the same `/api/site/students` scope and URL-state rules, shows explicit school-pick guidance if `/api/site/review-queue` returns `site_selection_required`, labels student-visible versus staff-only notes inside student-detail Reviews when the route provides those visibility values, and now restores Presentation section focus through the scoped `presentationFocus` section URL state.
- Administration: locally proven denied from `/api/site/programs`; Site Dashboard, Operations, and site-backed Readiness now keep leadership monitoring copy, the `Needs Attention` card keeps only role-safe Students and Operations drill-downs visible, the disclosed `Next Actions` card uses only Student Directory and Operations presets while unavailable Review Queue destinations stay summary-only, the disclosed `Recent Activity` card stays in-dashboard and read-only without exposing Audit navigation, and both read-only student detail and the new topbar student search now restore through the same scoped Student Directory path. Student-detail Reviews also explain role-scoped note visibility and keep admin-only context hidden, and the read-only Presentation section now restores the current focus through the same scoped URL.
- Program Teacher: locally proven denied from `/api/site/programs`; existing scoped program routes remain covered by prior local proof, Program Dashboard `Needs Attention` rows now open the existing Students, Review Queue, and Operations presets for exact scoped follow-up including mentor-meeting rows through the new `progressStatus=mentor_meeting_follow_up` Student Directory contract, `/api/site/students` now returns mentor-meeting follow-up rows only when the latest visible mentor meeting is `missed` or `makeup_required`, the Review Queue now shows explicit school-pick guidance if `/api/site/review-queue` returns `site_selection_required`, the Review Queue has clearer row-level review versus follow-up guidance plus row-level risk explanation copy without intervention wording, student detail now restores from the same scoped review/program URL with current tab state, the new topbar student search now reuses the same scoped `/api/site/students` search path, student-detail Reviews now explain when note visibility is limited to the current scoped detail, and the Presentation section now restores the current focus through the same scoped URL.
- Mentor: locally proven denied from `/api/site/programs`; assigned-student mentor routes remain covered by prior local proof, the top mentor metrics now route through the existing `mentorFocus` filters for all/revision/meeting/presentation views, mentor-opened student detail now restores from the same mentor-dashboard focus URL, student-detail Reviews now keep note labels role-scoped instead of exposing raw staff-only wording to mentor readers, and the mentor-visible Presentation section now restores the current focus through the same scoped URL.
- Viewer: locally proven denied from `/api/site/programs`; read-only Student Directory/detail routes still need hosted click-through, but local workspace coverage now restores read-only student detail from the same scoped directory URL, the topbar student search reuses that same read-only Student Directory scope, student-detail Reviews now explain that note visibility stays within the current detail scope while admin-only context remains hidden, and denied protected sections now say access is limited instead of implying the route failed.
- Student: locally proven denied from `/api/site/programs`; own-student routes still need a full current refresh.

## 8. Role-By-Role Forbidden Or Hidden Actions

- Global Admin: should not be collapsed into Site Admin or reduced to mentor/program-teacher scope.
- Site Admin: cannot manage another school and cannot use fake/unbacked program controls.
- Administration: cannot access Site Programs, security controls, account reset, or mutation controls.
- Program Teacher: cannot access Site Programs, global security, user management, or off-program student data.
- Mentor: cannot access Site Programs, self-assign students, or see unrelated student records.
- Viewer: cannot access Site Programs, review mutations, mentor-assignment mutations, or denied workspace sections.
- Student: cannot access Site Programs, staff-only notes, other-student data, or staff/admin controls.

## 9. Site Admin Programs Evidence

- First-load add path: verified locally. The local demo seed creates one active demo-owned catalog program, `Biotechnology`, that is not attached to the primary site, so it appears under Programs you can add on first load.
- Add/remove/restore: verified locally through `tests/site-programs.integration.test.mjs` and `scripts/prove-local-demo-workspace.mjs`.
- Visibility guard: verified locally through `tests/workspace-app.test.mjs` and `npm run verify:workspace-navigation`; Programs remains visible only for Global Admin and Site Admin.
- API denial guard: verified locally for Administration, Program Teacher, Mentor, Viewer, and Student through `tests/site-programs.integration.test.mjs` and local demo proof.
- Hosted status: pending. Do not claim hosted Programs proof until a hosted click-through is actually run.

## 10. Demo Data Notes

- The primary demo site still has all nine canonical CTE programs attached.
- The active `Biotechnology` catalog program is demo-owned by id and intentionally left unattached to the primary site so the add path is honest on first load.
- The proof cleans up its temporary addable-program mapping after route-level add/remove/restore checks so future first-load demos still start with an addable option.

## 13. Validation Results

- Latest Global Admin role-assignment closeout: `node --check workspace.js`, `node --test tests/workspace-app.test.mjs`, `npm run verify:functionality-language`, `node --test tests/functionality-language-audit.test.mjs`, `npm run verify:functionality-ux-automation`, `npm run check:production-surfaces`, `npm run typecheck`, `npm run test`, and `npm run check`.
- This closeout also passed `node -e "JSON.parse(require('fs').readFileSync('automation/state/functionality-ux-growth-state.json','utf8')); JSON.parse(require('fs').readFileSync('docs/progress/runs/2026-06-03-0208-role-assignment-multi-school-choice.json','utf8')); console.log('json ok')"` and `git diff --check`.
- Latest student-detail school-pick closeout: `node --check workspace.js` and `node --test tests/workspace-app.test.mjs` passed before docs/state closeout, with the full validation ladder recorded in `docs/functionality-ux-growth-ledger.md`.
