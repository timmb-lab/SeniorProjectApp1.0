# Senior Capstone MVP Requirements Catalog

Date: 2026-05-18

This catalog converts the master plan into MVP requirements. It is the operating map for the split Senior Capstone builders. The top-of-hour non-Figma builder must read this file after `docs/master-plan.md`, choose one bounded non-Figma requirement slice, update the status/evidence here when material progress happens, and ladder the next pass from the highest-risk incomplete requirement in its target area. The bottom-of-hour Figma-only builder owns `MVP-028` and may support other requirements only through concrete Figma handoffs that reduce implementation ambiguity. The daily summary is report-only, and the weekly strategy review adjusts this catalog from seven-day evidence when statuses, blockers, or priorities drift.

Status values:

- `not started`: no durable implementation/spec evidence yet.
- `designed`: Figma/Canva/spec evidence exists, but implementation is not started.
- `foundation started`: schema, route, test, config, or alpha proof exists but the production behavior is incomplete.
- `alpha active`: Day 7 alpha can demonstrate the behavior with seeded/demo personas or server-owned demo state.
- `mvp blocked`: account, credential, policy, connector, or platform blocker is stopping safe MVP completion.
- `mvp ready`: production-ready enough for pilot review, with tests and privacy/security evidence.

Functional requirement groupings:

- `requirements-audit`: requirement catalog, weekly goal audit, scope control, backlog hygiene, source-framework coverage.
- `backend-security-data`: auth, sessions, users, groups, roles, permissions, D1 schema, seed loaders, server authorization.
- `student-workflow-evidence`: student dashboard, proposal/research flow, progress updates, evidence metadata, mobile student path.
- `staff-review-mentor`: teacher review queue, comments, revision requests, approvals, mentor meetings, presentation workflow.
- `admin-ops-reporting`: admin provisioning, programs/cohorts/deadlines/templates, announcements, misc admin, exports, audit views.
- `deployment-qa`: GitHub-to-Cloudflare deployment, CI, test coverage, smoke checks, environment/secrets, backup/restore posture.
- `design-assets-handoff`: Figma product specs, Canva supporting imagery, component/state handoffs, guided prototype, implementation annotations.

Current active automation is recorded in `docs/automation-cadence.md`. The owner category column below remains a functional grouping; active delivery ownership now comes from the split builders:

- `senior-capstone-nonfigma-mvp-builder` owns implementation, tests, deployment, data/security, student/staff/admin workflows, repo-local docs/automation hardening, Canva-only support work, and exact blockers for `MVP-001` through `MVP-027`, `MVP-029`, and `MVP-030`.
- `senior-capstone-figma-product-builder` owns `MVP-028` and may support `MVP-004` through `MVP-027` only through route/data/permission annotations, functional state variants, screenshots, metadata verification, component/state handoffs, or exact Figma blockers.
- P0 production-experience overlays `MVP-031` through `MVP-040` are owned by the non-Figma builder for implementation/validation and supported by the Figma builder only through route/data/permission/state handoffs.
- The non-Figma builder may consume existing Figma evidence but may not spend runs doing direct Figma work.
- `MVP-029` Canva work stays outside the Figma lane except where design placement or relationship to a Figma handoff needs to be recorded.
- Figma is not production data and is not the source of truth for secure student records, accounts, evidence, audit logs, or dashboards.

## Category Summary

| Category | Primary MVP question | Current status | Next evidence needed |
| --- | --- | --- | --- |
| Product, privacy, and alpha boundary | Is the project aiming at a real secure hosted app and not a visual prototype? | `foundation started` | Pass the P0 production experience gate for both the role-aware app and Student/Teacher public website. |
| Identity, sessions, users, groups, roles | Can authorized users be created, scoped, signed in, and denied correctly? | `foundation started` | Add permission helpers/tests, account lifecycle, and hardened user/group assignment flows. |
| Programs, cohorts, requirements, deadlines | Are the nine CTE programs and source-PDF requirements first-class records? | `foundation started` | Add framework seed loader and requirement/deadline tests. |
| Student workspace, proposal/research, progress | Can a student complete the core capstone proposal path from dashboard to submission? | `alpha active` | Deepen guided sections, blocked-submit states, progress persistence, and mobile proof. |
| Private evidence and protected records | Are evidence artifacts private, linked to submissions, reviewable, and auditable? | `foundation started` | Implement Drive upload credentials/OAuth, access checks, and protected-evidence tests. |
| Reviews, revisions, approvals, mentor flow | Can staff review, request revision, approve, and mentor through scoped workflow? | `alpha active` | Convert alpha actions into production endpoints and tests for scopes/history. |
| Admin operations, announcements, exports, audit | Can admins manage operations and produce auditable exports without overexposing data? | `foundation started` | Implement admin surfaces/endpoints, scoped misc-admin checks, export records, and redaction. |
| Dashboards and reporting | Do dashboards derive from trusted server/database state and link to underlying records? | `alpha active` | Persist dashboard aggregates and add tests against stale/client-only counts. |
| Deployment, QA, observability, backup | Can the app be deployed, checked, monitored, and recovered safely? | `foundation started` | Verify each new post-push deployment, restore MCP/Wrangler path, broaden CI. |
| Figma, Canva, and handoff assets | Do visuals/specs reduce implementation ambiguity without becoming the source of truth? | `designed` | Keep Figma/Canva tied to route/data/permission gaps and stop broad polish until alpha gaps close. |

## P0 Production Experience Gate Requirements

These overlays define the real production deliverables Bryan wants. They do not replace the original MVP foundation requirements; they prevent alpha, smoke, preview, stakeholder-option, or generated-mirror work from being counted as the production experience.

| ID | Requirement | Owner category | Current evidence | Acceptance evidence needed | Status |
| --- | --- | --- | --- | --- | --- |
| MVP-031 | Enforce the P0 Production Experience Gate with two deliverables: role-aware production app and public Student/Teacher website. | `requirements-audit` | `docs/master-plan.md`, `docs/production-surface-registry.md`, `docs/source-materials/production-content-crosswalk.md`, and public Student/Teacher guide mode implementation in `app.js` / generated `public-companion/app.js`. | Both deliverables have canonical routes, production-safe copy, local validation, and source mapping evidence. | `foundation started` |
| MVP-032 | Identify and build the canonical role-aware production app route. | `backend-security-data` | D1/auth/API foundation exists; `alpha.html`, `account.html`, and `app-preview.html` are explicitly not canonical app routes. | Registry names the canonical route; route requires authenticated session and renders from D1-backed role/scope, not public persona switching. | `not started` |
| MVP-033 | Select app screens from authenticated user role and permission scope. | `backend-security-data` | Permission helpers, scoped APIs, alpha role modeling, and Figma role/state handoffs exist. | Tests prove student-own, mentor-assigned, program/cohort teacher, admin, misc-admin, role-missing, no-assignment, disabled/reset, and permission-denied screen behavior. | `foundation started` |
| MVP-034 | Keep canonical app copy production-safe. | `deployment-qa` | Production-surface checker scans production public/app/generated surfaces and excludes internal alpha/account/docs/tests/scripts. | Checker fails on alpha, QA, smoke, fake-account, prompt, builder, debug, token, raw storage ID, and preview-only copy on canonical production routes. | `foundation started` |
| MVP-035 | Add the public Student Guide / Teacher Guide website toggle. | `student-workflow-evidence` | `app.js` renders a top-banner guide segmented control with `Viewing: Student Guide`, `Viewing: Teacher Guide`, `Switch to Student Guide`, `Switch to Teacher Guide`, `aria-pressed`, and public `senior-capstone-guide-mode` preference storage; `public-companion/app.js` was rebuilt from source and `tests/account-and-evidence-access.test.mjs` covers the markers. | Browser/mobile smoke should confirm the toggle is visually stable after an active browser pane is available; future public-site changes must keep the focused source test and production-surface checker passing. | `foundation started` |
| MVP-036 | Enforce no-hidden-core-content on the public website. | `requirements-audit` | Public guide has visible timelines/cards/checklists, and the home page now keeps Student Guide and Teacher Guide summary cards visible at the same time instead of hiding core directions behind auth or collapsed panels. | Required directions, due dates, rubrics, responsibilities, and actions are visible or summarized without requiring details-panel expansion across the full public route set. | `foundation started` |
| MVP-037 | Cover Student Guide and Teacher Guide source content. | `requirements-audit` | Source documents are extracted and mapped in `docs/source-materials/production-content-crosswalk.md`; the implemented guide cards visibly summarize Senior Remind/class website, program expectations, Core Concept Proposal, Research Proposal Challenge, mentor meetings, presentation/celebration, thank-you/mentor note, reflections, portfolio, rubrics, recognition, May 5 archive, proposal review, Google Form grading, check-out/check-in, first-10-minutes rule, paper rubric option, ingredients, and intervention signals. | Expand page-level Student/Teacher emphasis beyond the home-page guide cards and keep crosswalk statuses current as source slices become full website/app screens. | `foundation started` |
| MVP-038 | Maintain source-document crosswalk as implementation evidence. | `requirements-audit` | `docs/source-materials/production-content-crosswalk.md` maps the booklet, Senior Guide, Mentor Teacher Guide, Research Proposal Challenge, and Cycle Linked Document. | Crosswalk statuses are updated whenever website/app source coverage changes. | `source-mapped` |
| MVP-039 | Validate production-copy safety for app, public website, and generated public output. | `deployment-qa` | `scripts/check-production-surfaces.mjs` now checks exact Student/Teacher guide toggle markers; `scripts/check-generated-output-drift.mjs`; focused public-surface source test; rebuilt `public-companion/` output. | Aggregate checks fail on forbidden production-surface copy, normal-nav links to alpha/account/internal smoke, public-companion alpha/account/API leaks, stale generated output, and missing guide-toggle source markers. | `foundation started` |
| MVP-040 | Prevent stakeholder options from being confused with Student/Teacher guide modes. | `requirements-audit` | `docs/stakeholder-option-lifecycle.md`, registry classifications, and site option checker. | Stakeholder options remain review artifacts unless Bryan promotes one through the lifecycle; Student/Teacher guide mode is implemented in the canonical public website, not `stakeholder-options/**`. | `foundation started` |

## Core Requirements

| ID | Requirement | Owner category | Current evidence | Acceptance evidence needed | Status |
| --- | --- | --- | --- | --- | --- |
| MVP-001 | Build a GitHub-to-Cloudflare hosted Senior Capstone app, not a static guide or Figma-only prototype. | `deployment-qa` | `docs/backend-setup.md`, `wrangler.jsonc`, Cloudflare Pages artifact, production deployment `2aadfa71`, live alpha route, `/api/alpha/state`, `docs/production-deployment-policy.md`, `docs/production-surface-registry.md`, `docs/production-predeploy-checklist.md`, deterministic route inventory in `docs/generated/production-route-inventory.md`, and P0 gate/crosswalk evidence in `docs/master-plan.md` plus `docs/source-materials/production-content-crosswalk.md`. | Verify each new post-push deployment, record URL/API status, keep canonical production deploy targets documented, designate/build the role-aware production app route, run the predeploy/P0 validation gates, and commit exact blocker if Cloudflare auth or Wrangler blocks. | `foundation started` |
| MVP-002 | Preserve the Day 7 alpha account exception while clearly labeling seeded/demo personas as not production auth. | `requirements-audit` | `docs/alpha-week-framework.md`, `docs/alpha-runbook.md`, `alpha.html`, `alpha.js`, internal QA page labels, production navigation with alpha/account links removed, and `docs/alpha-account-deployment-decision.md`. | Bryan chooses whether alpha/account stay deployed unlinked, move behind a gate, or are removed before pilot; walkthrough labels account shortcuts; no alpha copy claims pilot readiness or real account safety; production navigation stays free of alpha/smoke routes. | `alpha active` |
| MVP-003 | Exclude real student data from alpha and prevent private records from entering public/static assets. | `requirements-audit` | Alpha runbook, checker guardrails, `scripts/check-production-surfaces.mjs`, `scripts/check-generated-output-drift.mjs`, generated output source-of-truth policy, public companion output without internal alpha/account/API redirects, and the P0 forbidden production-copy list in `docs/master-plan.md`. | Contract check plus audit review confirms no real student names, private evidence, credentials, staff-only notes, fake-account UX, internal QA links, raw storage IDs, prompt/builder copy, or public-companion API redirects on production-classified public surfaces. | `foundation started` |
| MVP-004 | Provide hardened username/password pilot auth behind a narrow boundary that can later swap to SSO. | `backend-security-data` | `functions/api/auth/*`, `migrations/0001_foundation.sql`, auth setup notes, verified first-admin bootstrap for `bryan.timm89@gmail.com`, setup-key removal, login/session proof, `bootstrap_admin_created` audit event, fake `.test` role accounts including admin, and auth edge-state integration coverage in `tests/auth-login.integration.test.mjs` (logout, session expiry, reset-required). | Add account lifecycle, password reset endpoints, invitation/import, credential rotation, and broader role-scope tests before pilot use. | `foundation started` |
| MVP-005 | Store users, credentials, sessions, login attempts, roles, and role assignments in D1. | `backend-security-data` | D1 migration creates auth/account tables; production D1 currently has 1 real admin plus 4 fake `.test` role accounts and `/api/health` reports `userCount=5`; `tests/auth-login.integration.test.mjs` covers active/disabled/reset-required/invalid login plus session expiry and logout revocation. | D1 read/write tests cover active, disabled, reset-required, invalid login, and session expiry states (see `tests/auth-login.integration.test.mjs`). | `foundation started` |
| MVP-006 | Support student, mentor, program teacher, admin, and misc admin roles with default-deny permissions. | `backend-security-data` | Migration seeds 5 roles; alpha persona switcher models scopes; fake `.test` student, mentor, program-teacher, admin, and misc-admin accounts are represented in the seed endpoint and smoke checklist; account smoke verifies selected account/session/expected role scope; executable permission-helper tests in `tests/permissions-access.test.mjs` cover the default-deny student access matrix and require both the `mentor` role and an active mentor assignment for mentor-scoped access; teacher scope checks default-deny empty `program_teacher` scope IDs (no null/empty group matching); admin provisioning route `/api/admin/mentor-assignments` requires admin and blocks creating mentor assignments unless the mentor has the `mentor` role and the student has the `student` role (audited), with integration coverage in `tests/admin-mentor-assignments.integration.test.mjs`. | Permission helper tests cover student-own, mentor-assigned, teacher program/cohort, admin all, misc-admin narrow, and unauthorized denial. | `foundation started` |
| MVP-007 | Let admins manage users, groups, programs, cohorts, mentor assignments, teacher assignments, and role scopes. | `admin-ops-reporting` | Figma node `48:2`; D1 tables exist; admin test account seeding endpoint `functions/api/admin/test-accounts.ts` and `tests/test-account-seed.test.mjs` exist; admin mentor assignment endpoints `/api/admin/mentor-assignments` now support list (GET), upsert/activate (POST), and deactivate (DELETE, audited) with deterministic integration coverage in `tests/admin-mentor-assignments.integration.test.mjs`; admin role-assignment endpoint `/api/admin/role-assignments` now supports list (GET), create/upsert (POST), and remove (DELETE, audited) for `user_roles` (including validated `program_teacher` program/cohort scopes) with integration coverage in `tests/admin-role-assignments.integration.test.mjs`. | Admin endpoints/surfaces create/update/import assignments, detect duplicates, audit role changes, and block misc-admin broad access. | `foundation started` |
| MVP-008 | Represent all nine required CTE programs as first-class records and filters. | `backend-security-data` | Migration seeds all nine programs. | UI/API filters and dashboard aggregates can scope by every required program. | `foundation started` |
| MVP-009 | Convert source-PDF workflow into app-native requirements, sections, quality checks, deadlines, review gates, and dashboard signals. | `requirements-audit` | `data/capstone-framework.json`; schema migration `migrations/0002_framework_seed.sql`; generated seed migration `migrations/0003_framework_seed_data.sql`; seed generator `scripts/framework-seed.mjs`; contract tests `tests/framework-seed.test.mjs` + `tests/framework-seed-migration.test.mjs`; admin test-account seeding now references seeded requirement ID `req-proposal-draft` (replacing `req-alpha-proposal`); D1 local migration + seed verification script `scripts/verify-framework-seed-d1.ps1` (local counts verified: requirements=16, deadlines=24, review_gates=4, quality_checks=20); backlog `SC-001`. | Apply migrations to **remote** D1 and verify seeded counts by query (Wrangler needs `CLOUDFLARE_API_TOKEN` for non-interactive remote runs). | `foundation started` |
| MVP-010 | Track student progress records and status history from trusted server/database state. | `student-workflow-evidence` | D1 tables; Figma node `61:2`; alpha state transitions; `/api/submissions/:id/submit`, `/api/submissions/:id/evidence`, `/api/reviews/:submissionId/decision`, and `/api/reviews/:submissionId/history` update/read submissions, progress records, status history, evidence metadata, and audit events. | Add stale-write handling and broader route tests; dashboard counts derive from DB/server state. | `foundation started` |
| MVP-011 | Provide student dashboard with phase, next action, due items, evidence needs, revision state, and program context. | `student-workflow-evidence` | Alpha student workspace, server-derived next-step guidance, reviewer checks, copyable walkthrough summary, Figma student desktop/mobile frames, and D1-backed `/api/student/dashboard`. | Mobile and desktop walkthrough shows live state changes after submit/revision/approval. | `alpha active` |
| MVP-012 | Provide guided proposal/research workflow with sections, evidence requirements, draft, submit, revise, and resubmit. | `student-workflow-evidence` | Alpha actions and Figma guided proposal `3:154`; alpha state machine now blocks out-of-order submit/resubmit/approve/revision transitions and duplicate meeting/export actions; production `/api/submissions/:id/submit` blocks submission when no evidence artifacts are attached (409 `submission_missing_evidence`) and audits the blocked attempt. | Server-owned guided sections persist drafts/submissions and expose blocked-submit reasons before students submit. | `alpha active` |
| MVP-013 | Validate evidence metadata and support private evidence links/uploads through Google Drive repository path. | `student-workflow-evidence` | D1 `evidence_artifacts`, Drive root/index artifacts, alpha evidence URL validation, health readiness fields, and `/api/submissions/:id/evidence` for scoped HTTPS evidence-link metadata attachment, plus service-account JWT Drive token exchange helper (`functions/_lib/google-drive.ts`) and authenticated/audited `/api/evidence/drive-probe` route with integration tests covering missing config/creds, token exchange failure, and Drive access-denied responses. Drive-backed evidence file-byte upload route `/api/submissions/:id/evidence/upload` now supports multipart uploads (≤5MB) and resumable uploads (>5MB, capped at 20MB) -> Drive -> writes `evidence_artifacts.drive_file_id`, with integration tests covering provider forbidden/upload failure plus the resumable path. Access-controlled streaming download route `/api/evidence/:id/download` has integration tests that stub fetch() for token exchange/provider failures and assert audit events + no storage-id leaks. Account smoke page now includes an authenticated Drive file-byte upload + download panel (`account.html`, `account.js`) for manual verification without committing test passwords. | Configure Cloudflare Pages secrets for Drive, then verify real upload + download against the configured evidence root folder (use the account smoke page Drive panel for the end-to-end check) including one >5MB upload to exercise the resumable path; follow on with chunked resume (308) + retry/backoff handling if Drive provider errors surface in real usage. | `foundation started` |
| MVP-014 | Keep evidence artifacts access-controlled and auditable, including denied-access events. | `backend-security-data` | Figma nodes `37:2`, `43:149`, `56:2`; D1 evidence tables; `/api/evidence/:id/check-access` checks current session scope with `canAccessStudent`, audits unauthorized/denied/allowed/missing access, and withholds Drive storage IDs; `tests/permissions-access.test.mjs` exercises `canAccessStudent` across the role matrix used by evidence access checks (including empty scope-id denial); `tests/evidence-check-access.integration.test.mjs` executes the evidence access route (401/403/404/200) and asserts audit-event writes plus storage-id redaction (including empty program-teacher scope-id denial); `tests/evidence-drive-file.integration.test.mjs` now covers `/api/evidence/:id/download` (401/403/404/409/502/200) including student/mentor/program-teacher/admin allow-path streaming, misc-admin denial, and role-scope audit assertions. | Tests prove student own access, mentor assigned access, program teacher scope, admin scope, misc-admin narrowing, unauthorized denial, and audit events. | `foundation started` |
| MVP-015 | Provide program teacher review queue, detail, comments, revision request, approval, status history, and dashboard count updates. | `staff-review-mentor` | Alpha teacher review actions; Figma nodes `31:2`, `61:2`; `/api/teacher/review-queue`, `/api/reviews/:submissionId/decision`, and `/api/reviews/:submissionId/history` persist/list review work with teacher/admin scope checks; `/api/reviews/:submissionId/decision` now supports `comment_only` review records that persist into `comments` without changing submission status; integration test `tests/review-loop.integration.test.mjs` executes student submit -> teacher comment -> teacher revision -> student resubmit (version++) -> teacher approval and asserts queue membership + persisted `reviews` + `comments` + `status_history`; `account.html`/`account.js` now include a review-history smoke panel that renders comments, review decisions, status changes, and versions from `/api/reviews/:submissionId/history`; the primary alpha console now preserves and renders teacher comments, decisions, status events, and version snapshots from server-owned alpha state with a storage-identifier guard. | Confirm the hosted alpha/account walkthrough completes student -> teacher comment/revision -> resubmission -> approval without code edits, with immutable history visible in the UI and dashboards updating from DB/server state. | `alpha active` |
| MVP-016 | Preserve immutable review history and prior submission versions. | `staff-review-mentor` | Figma node `37:2`; alpha review history timeline; D1 migration `migrations/0005_submission_versions.sql`; `/api/submissions/:id/submit` writes immutable `submission_versions` snapshots with evidence metadata on submit/resubmit; `/api/reviews/:submissionId/history` returns review decisions, comments, status history, and version snapshots; `tests/review-loop.integration.test.mjs` covers comments plus two preserved versions across revision/resubmission/approval; the account smoke UI now renders returned comments and version snapshots while blocking raw Drive storage identifiers, with regression coverage in `tests/account-and-evidence-access.test.mjs`; `functions/_lib/alpha-flow-model.js` and `alpha.js` now mirror that history shape in the primary alpha walkthrough, backed by `tests/alpha-flow.test.mjs` and `scripts/check-alpha-contract.mjs`. | Apply/verify `0005_submission_versions.sql` in remote D1 when `CLOUDFLARE_API_TOKEN` is available, then confirm the hosted account/alpha UI renders version history without exposing Drive storage IDs. | `foundation started` |
| MVP-017 | Support mentor assigned-student visibility, meeting attendance, make-up requirements, outline approval cues, presentation slot risk, and scoped actions. | `staff-review-mentor` | Alpha mentor actions; Figma node `78:2`; backlog `SC-004`; `/api/mentor/assigned` exposes active mentor assignments with submission status and evidence counts; meeting persistence migration `migrations/0004_mentor_meetings.sql`; authenticated mentor meeting list/create route `/api/mentor/meetings` with scope enforcement + audit events and integration coverage `tests/mentor-meetings.integration.test.mjs`; presentation slot migration `migrations/0006_presentation_slots.sql`; `/api/presentation-slots` lists only role-scoped slots, limits scheduling to admin/program-teacher staff, blocks same-location overlapping slots, and audits denied/conflict/scheduled outcomes with integration coverage in `tests/presentation-slots.integration.test.mjs`. | Apply/verify `0006_presentation_slots.sql` remotely when `CLOUDFLARE_API_TOKEN` is available, add presentation check-out/check-in endpoints, persist outline approval gates, and surface presentation status/risk in mentor/student dashboards. | `foundation started` |
| MVP-018 | Support admin overview, deadlines, templates, announcements, overrides, audit/activity, exports, and archive controls. | `admin-ops-reporting` | Alpha admin actions; Figma nodes `48:2`, `69:2`; D1 tables for deadlines, announcements, exports, audit; `/api/admin/announcements`, `/api/admin/exports/student-archive`, `/api/exports/:id/download`, and `/api/admin/audit-events` cover the first persisted admin operations. | Admin UI surfaces persist operations, require override reasons, redact audit details, generate real export artifacts, and record export lifecycle. | `foundation started` |
| MVP-019 | Provide misc-admin narrow read/reporting view without broad admin powers. | `admin-ops-reporting` | Alpha misc-admin report and restricted approval denial; `/api/reports/readiness` is admin/misc-admin only and returns aggregate counts without names, emails, or student-level rows. | Tests show misc-admin can run allowed reports but cannot approve, export protected archives, change roles, or view out-of-scope evidence. | `alpha active` |
| MVP-020 | Maintain audit logs for sensitive actions: login, role changes, progress/status changes, evidence access, review decisions, overrides, exports, and permission denials. | `admin-ops-reporting` | D1 `audit_events`; alpha activity timeline; Figma node `69:2`; `/api/admin/audit-events` is admin-only and redacts sensitive metadata keys; announcement create, mentor assignment provisioning/denials (`/api/admin/mentor-assignments`), archive queue/download checks, readiness report views, evidence checks, and review decisions write audit events. | Audit API/view filters immutable events, redacts sensitive metadata, and records denied access. | `foundation started` |
| MVP-021 | Generate dashboard aggregates from trusted state for students, mentors, teachers, admins, and misc admins. | `student-workflow-evidence` | Alpha dashboard metrics; Figma node `61:2`. | Aggregates are computed from DB/server state, not client-only state, and link to underlying records. | `alpha active` |
| MVP-022 | Support exports/archives, including May 5 student archive package and scoped signed download behavior. | `admin-ops-reporting` | D1 `exports`; Figma node `69:2`; alpha export queue; `/api/admin/exports/student-archive` queues admin archive records and `/api/exports/:id/download` enforces admin/student-scope access while signed URL generation remains pending. | Export artifact generation exists with scoped authorization, signed expiry or safe Drive link behavior, audit events, and retention notes. | `foundation started` |
| MVP-023 | Provide announcement surfaces for staff/admin updates without adding student-to-student messaging. | `admin-ops-reporting` | D1 `announcements`; master-plan guardrail; `/api/admin/announcements` creates audited scoped announcements and `/api/announcements` lists only currently visible signed-in-user announcements. | Admin/staff UI create/list/read flow exists; audit confirms no chat, peer DMs, or social feed. | `foundation started` |
| MVP-024 | Preserve mobile-safe student path with no horizontal overflow. | `student-workflow-evidence` | Alpha CSS and earlier Edge mobile render proof; Figma node `56:2`. | Browser/mobile smoke proof after current alpha route, including revision/evidence states. | `foundation started` |
| MVP-025 | Maintain CI and local validation for alpha contract, state machine, syntax, type checks, and future permission tests. | `deployment-qa` | `npm run check`, `scripts/check-alpha-contract.mjs`, `scripts/check-production-surfaces.mjs`, `scripts/inventory-production-routes.mjs`, `scripts/check-generated-output-drift.mjs`, `tests/alpha-flow.test.mjs`, `tests/test-account-seed.test.mjs`, `.github/workflows/*.yml`. | CI green on GitHub, production-surface leak checks pass, route inventory is current, generated output drift checks pass, P0 production-copy/nav/toggle-pending signals are explicit, and local/Node REPL fallback is documented when local shell lacks Node/npm/Wrangler. | `foundation started` |
| MVP-026 | Verify Cloudflare Pages deployment, D1 binding, env vars/secrets, and `/api/alpha/state` after pushes. | `deployment-qa` | Production deployment `2aadfa71` verified `/alpha.html`, `/api/alpha/state`, `/api/health`, D1 first-admin state, login/session, and bootstrap 403 after setup-key removal. Production deployment `c7908d04` for commit `dc2f82a` verified the admin-only test-account seed endpoint; `/api/health` now reports `userCount=5`, all four fake role accounts log in, unauthenticated seed calls return 401, and student seed calls return 403. Health route now reports evidence root/index and Drive credential readiness booleans for smoke checks. `docs/production-predeploy-checklist.md` now defines static vs live Cloudflare gates and records `LIVE_CLOUDFLARE_BLOCKED_NO_TOKEN` when the token is absent. | After this predeploy-gate commit lands, verify the new production/preview deployment or record the exact connector/Wrangler blocker; live Pages/D1 verification requires `CLOUDFLARE_API_TOKEN`. | `foundation started` |
| MVP-027 | Maintain backup/export posture, retention notes, secrets discipline, and no credential commits. | `deployment-qa` | `.gitignore`, backend setup notes, artifact registry, alpha runbook, `docs/production-predeploy-checklist.md`, and `docs/custom-domain-cutover-checklist.md`. | Backup/restore, rollback, custom-domain cutover, and secret rotation checklists exist; scan confirms no secrets or real records committed. | `foundation started` |
| MVP-028 | Keep Figma as functional product-design source for route/data/permission/states, not production data. | `design-assets-handoff` | Owned by the bottom-of-hour Figma-only product builder. Active file `z4t4tFPAKrMDh6pIYOeEw6`, nodes `18:2`, `31:2`, `37:2`, `43:2`, `48:2`, `56:2`, `61:2`, `69:2`, `78:2`, and prototype page `98:2`; 2026-05-20 alignment updated and verified review-history prototype nodes `98:9` and `98:10` against `/api/reviews/:submissionId/history`, `reviews`, `comments`, `status_history`, `submission_versions`, scoped permissions, and storage-ID redaction; 2026-05-20 handoff node `98:17` now records `review_history_consumed_at`, the primary alpha-console consumption state, next mentor/presentation/admin rebuild focus, and verified screenshot/metadata. | Figma-only runs produce functional handoffs, route/data/permission annotations, state variants, screenshots/metadata, or exact blockers; non-Figma runs consume existing Figma evidence but do not call Figma tools. No broad Figma polish while Day 7 alpha gaps are open unless a specific implementation ambiguity exists. | `designed` |
| MVP-029 | Keep Canva as supporting-image source with live-text and privacy discipline. | `design-assets-handoff` | Canva folder/artifacts and open empty-state handoffs. Canva is owned by the non-Figma builder when it does not require direct Figma work. | Asset specs include placement, dimensions, alt text, no private data, and live-text guidance; assets are used only where helpful. The Figma-only lane may only document Canva placement/relationship to a Figma handoff, not perform Canva work. | `designed` |
| MVP-030 | Keep the 100-pass target honest with daily summaries and weekly evidence-based calibration. | `requirements-audit` | Split cadence docs, top-of-hour non-Figma builder prompt, bottom-of-hour Figma-only product builder prompt, daily summary automation, weekly strategy review automation, the production-surface 100-pass report, the predeploy-gate follow-up batch, and the P0 production app/website gate. | Daily summary reports last-24-hour accepted progress; weekly strategy review counts accepted passes and updates only this project master plan/memory/catalog/backlog/cadence records from evidence. Split-builder accepted passes require durable implementation, design handoff, validation, artifact, exact blocker evidence, or production-surface/deploy-gate validation that removes deploy ambiguity. Alpha/smoke/preview/stakeholder-only work does not count as production-experience completion. | `foundation started` |

## Current Highest-Risk Requirements

1. `MVP-032` and `MVP-033`: the canonical role-aware production app route is still TBD; alpha/account/app-preview are not the production app.
2. `MVP-036` and `MVP-037`: the public website now has the Student Guide / Teacher Guide top-banner mode, but still needs fuller no-hidden-core-content coverage proof across every public route.
3. `MVP-006` and `MVP-014`: permission/protected-evidence tests are not broad enough for real student records.
4. `MVP-013`: Drive upload/download code exists, but live Cloudflare secrets and real upload/download verification are still pending.
5. `MVP-009`: source-framework seed loader and local D1 verification exist; remote D1 apply/verification is pending on `CLOUDFLARE_API_TOKEN`.
6. `MVP-015` through `MVP-018`: alpha demonstrates workflow actions and several production endpoints exist, but canonical role-aware UI surfaces and full workflow depth remain incomplete.
7. `MVP-004`: account lifecycle, invitation/import, password reset endpoints, credential rotation, and role-scope hardening remain incomplete after first-admin bootstrap (logout/session expiry/reset-required tests now exist).
8. `MVP-026`: each new post-push deployment still needs verification; live Cloudflare Pages/D1 proof remains blocked when `CLOUDFLARE_API_TOKEN` is absent.

## Laddering Rule

The split builders must:

1. Pick the highest-risk incomplete requirement in the lane's target area.
2. Prefer implementation, tests, deployment evidence, a verified Figma handoff, or an exact blocker over planning.
3. Update this catalog only when a requirement's status, evidence, blocker, or next acceptance check materially changes.
4. Write the structured run manifest with the requirement IDs touched.
5. Commit and push every repo change, or commit a blocker record that names the exact unavailable account/tool/policy step.
