# Capstone Project MVP Requirements Catalog

Date: 2026-05-18

This catalog converts the master plan into MVP requirements. It is the operating map for product-readiness work selected by the Functionality UX Upgrade automation. Each automation run should read this file after `docs/master-plan.md`, choose a bounded safe improvement, update the status/evidence here when material progress happens, and ladder each next pass from the highest-risk incomplete requirement in its target area.

Status values:

- `not started`: no durable implementation/spec evidence yet.
- `designed`: Figma/Canva/spec evidence exists, but implementation is not started.
- `foundation started`: schema, route, test, config, or alpha proof exists but the production behavior is incomplete.
- `alpha active`: Day 7 alpha can demonstrate the behavior with seeded/demo personas or server-owned demo state.
- `mvp blocked`: account, credential, policy, connector, or platform blocker is stopping safe MVP completion.
- `mvp ready`: production-ready enough for pilot review, with tests and privacy/security evidence.

Functional requirement groupings:

- `requirements-audit`: requirement catalog, weekly goal audit, scope control, backlog hygiene, source-framework coverage.
- `backend-security-data`: auth, sessions, users, groups, roles, permissions, D1 schema, seed loaders, server authorization.
- `student-workflow-evidence`: student dashboard, proposal/research flow, progress updates, evidence metadata, mobile student path.
- `staff-review-mentor`: teacher review queue, comments, revision requests, approvals, mentor meetings, presentation workflow.
- `admin-ops-reporting`: admin provisioning, programs/cohorts/deadlines/templates, misc admin, exports, audit views. Announcement surfaces are removed from the active MVP.
- `deployment-qa`: GitHub-to-Cloudflare deployment, CI, test coverage, smoke checks, environment/secrets, backup/restore posture.
- `design-assets-handoff`: Figma product specs, Canva supporting imagery, component/state handoffs, guided prototype, implementation annotations.

Current active automation is recorded in `docs/automation-cadence.md`. The owner category column below remains a functional grouping; it is not a scheduler or automation-lane declaration. The only active automation contract is Functionality UX Upgrade (`functionality-ux-upgrade-hourly`).

## Category Summary

| Category | Primary MVP question | Current status | Next evidence needed |
| --- | --- | --- | --- |
| Product, privacy, and alpha boundary | Is the project aiming at a real secure hosted app and not a visual prototype? | `foundation started` | Pass the P0 production experience gate for both the role-aware app and Student/Teacher public website. |
| Identity, sessions, users, groups, roles | Can authorized users be created, scoped, signed in, and denied correctly? | `foundation started` | Add account lifecycle depth, deploy Google Workspace SSO after tenant/OAuth setup, and keep fake `.test` local fallback green. |
| Programs, cohorts, requirements, deadlines | Are the nine CTE programs and source-PDF requirements first-class records? | `foundation started` | Add framework seed loader and requirement/deadline tests. |
| Student workspace, proposal/research, progress | Can a student complete the core capstone proposal path from dashboard to submission? | `alpha active` | Deepen guided sections, blocked-submit states, progress persistence, and mobile proof. |
| Private evidence and protected records | Are evidence artifacts private, linked to submissions, reviewable, and auditable? | `foundation started` | Broaden browser permission-state proof, upload progress/retry handling, Google Docs export cases, and remaining protected endpoint families after hosted fake-account upload/download proof. |
| Reviews, revisions, approvals, mentor flow | Can staff review, request revision, approve, and mentor through scoped workflow? | `alpha active` | Convert alpha actions into production endpoints and tests for scopes/history. |
| Admin operations, exports, audit | Can admins manage operations and produce auditable exports without overexposing data? | `foundation started` | Keep the new admin command center backed by D1 counts, deepen role-management surfaces, scoped misc-admin checks, export records, and redaction. |
| Dashboards and reporting | Do dashboards derive from trusted server/database state and link to underlying records? | `foundation started` | Hosted proof for D1-backed admin/program-teacher/mentor dashboards, plus source-record drilldowns and stale/conflict transition checks. |
| Deployment, QA, observability, backup | Can the app be deployed, checked, monitored, and recovered safely? | `foundation started` | Keep Cloudflare/D1 and Drive live checks passing, and broaden CI/account lifecycle/hosted browser coverage. |
| Figma, Canva, and handoff assets | Do visuals/specs reduce implementation ambiguity without becoming the source of truth? | `designed` | Keep Figma/Canva tied to route/data/permission gaps and stop broad polish until alpha gaps close. |

## P0 Production Experience Gate Requirements

These overlays define the real production deliverables Bryan wants. They do not replace the original MVP foundation requirements; they prevent alpha, smoke, preview, stakeholder-option, or generated-mirror work from being counted as the production experience.

| ID | Requirement | Owner category | Current evidence | Acceptance evidence needed | Status |
| --- | --- | --- | --- | --- | --- |
| MVP-031 | Enforce the P0 Production Experience Gate with two deliverables: Capstone Project role-aware product app and East Tech Student/Teacher guide. | `requirements-audit` | `docs/master-plan.md`, `docs/production-surface-registry.md`, `docs/source-materials/production-content-crosswalk.md`, public Student/Teacher guide mode implementation in `app.js` / generated `public-companion/app.js`, and product/domain decision lock for `thecapstoneproject.com`. | Both deliverables have canonical routes, production-safe copy, local validation, target/current domain state, and source mapping evidence without claiming unverified live state. | `foundation started` |
| MVP-032 | Identify and build the canonical role-aware production app route. | `backend-security-data` | D1/auth/API foundation exists; `workspace.html`, `workspace.js`, and `workspace.css` now provide the canonical authenticated workspace route, while `alpha.html`, `account.html`, and `app-preview.html` remain non-canonical. `docs/generated/production-route-inventory.md` now classifies `workspace.html` as `production` / `conditional` for `senior-capstone-app`, and `tests/production-workflow-source.test.mjs` locks that classification so the canonical app route cannot silently fall back to `unknown`. `tests/workspace-app.test.mjs` verifies the route avoids local record storage/dev copy, references the real auth/upload/admin APIs, and executes workspace rendering for session-expired, account-disabled, reset-required, role-pending, permission-denied, no-active-assignment, self-service password change, archive, presentation, evidence download/open-link actions, and admin import states; it also proves one-time setup output is memory-only and disappears after refresh while non-admin import UI is gated. `tests/workspace-browser-smoke.test.mjs` verifies signed-out, local credential-backed student, upload-blocker, unsupported-upload, logout, role-route behavior, mentor no-assignment behavior, and local admin import/reset-first proof against local Pages/D1 when the ignored credential file is provided. In-app browser smoke verified signed-out and signed-in student workspace UI, evidence-link submission, logout, safe copy, role-pending no-role UI, no-active-assignment mentor UI, and zero console errors. Live signed-out and fake student signed-in checks passed for the hosted route. On 2026-05-21, hosted `workspace.js` returned the admin import UI markers `data-admin-action="import-users"`, `/api/admin/users/import`, and `data-admin-import-result="one-time-setup-passwords"` on poll 2 after push; later hosted `workspace.js` returned `data-evidence-download="file"` on poll 3 after commit `d0fe1f6`. | Route must keep rendering from D1-backed role/scope, not public persona switching. Live hosted proof is still needed for the newest account-state/no-assignment markers and a section-level permission-denied browser state. | `foundation started` |
| MVP-033 | Select app screens from authenticated user role and permission scope. | `backend-security-data` | Permission helpers, scoped APIs, alpha role modeling, Figma role/state handoffs, and the authenticated workspace shell now select student, mentor, teacher/admin, admin user-import, misc-admin/admin readiness, and signed-in overview sections from `/api/auth/me` roles/scopes. Upload-route denial tests prove non-student roles and another student's submission cannot use the student upload path. Local credential-backed smoke now verifies student, `program_teacher`, `mentor`, unassigned `mentor`, `admin`, `misc_admin`, no-role route outcomes, admin import permission denial for misc-admin, and imported student role readback after reset against local D1. The workspace now renders explicit `data-workspace-state` markers for `session-expired`, `account-disabled`, `reset-required`, `role-pending`, `permission-denied`, and `no-active-assignment`; source/VM tests execute all of those states, and in-app browser verified no-role role-pending plus unassigned mentor no-assignment states locally. `tests/workspace-app.test.mjs` now verifies the admin-only import tab, one-time setup password output path, memory-only refresh clearing, non-admin import UI gate, and evidence download/external-link actions without storage identifiers. Live `check:drive:live` verifies the hosted workspace evidence-download marker before upload/download proof. | Browser/UI tests still need a live hosted section-level permission-denied state and post-deploy account-state/no-assignment proof. | `foundation started` |
| MVP-034 | Keep canonical app copy production-safe and school-agnostic. | `deployment-qa` | Production-surface checker scans production public/app/generated surfaces plus canonical workspace files, excludes internal alpha/account/docs/tests/scripts, and blocks East Tech/Titan leakage from workspace files. Workspace copy now uses Capstone Project naming. | Checker fails on alpha, QA, smoke, fake-account, prompt, builder, debug, token, raw storage ID, preview-only copy, and East Tech/Titan leakage on canonical product routes. | `foundation started` |
| MVP-035 | Add the public Student Guide / Teacher Guide website toggle. | `student-workflow-evidence` | `app.js` renders a top-banner guide segmented control with `Viewing: Student Guide`, `Viewing: Teacher Guide`, `Switch to Student Guide`, `Switch to Teacher Guide`, `aria-pressed`, and public `senior-capstone-guide-mode` preference storage; `public-companion/app.js` was rebuilt from source and `tests/account-and-evidence-access.test.mjs` covers the markers. | Browser/mobile smoke should confirm the toggle is visually stable after an active browser pane is available; future public-site changes must keep the focused source test and production-surface checker passing. | `foundation started` |
| MVP-036 | Enforce no-hidden-core-content on the public website. | `requirements-audit` | Public guide has visible timelines/cards/checklists, and the home page now keeps Student Guide and Teacher Guide summary cards visible at the same time instead of hiding core directions behind auth or collapsed panels. | Required directions, due dates, rubrics, responsibilities, and actions are visible or summarized without requiring details-panel expansion across the full public route set. | `foundation started` |
| MVP-037 | Cover Student Guide and Teacher Guide source content. | `requirements-audit` | Source documents are extracted and mapped in `docs/source-materials/production-content-crosswalk.md`; the implemented guide cards visibly summarize Senior Remind/class website, program expectations, Core Concept Proposal, Research Proposal Challenge, mentor meetings, presentation/celebration, thank-you/mentor note, reflections, portfolio, rubrics, recognition, May 5 archive, proposal review, Google Form grading, check-out/check-in, first-10-minutes rule, paper rubric option, ingredients, and intervention signals. | Expand page-level Student/Teacher emphasis beyond the home-page guide cards and keep crosswalk statuses current as source slices become full website/app screens. | `foundation started` |
| MVP-038 | Maintain source-document crosswalk as implementation evidence. | `requirements-audit` | `docs/source-materials/production-content-crosswalk.md` maps the booklet, Senior Guide, Mentor Teacher Guide, Research Proposal Challenge, and Cycle Linked Document. | Crosswalk statuses are updated whenever website/app source coverage changes. | `source-mapped` |
| MVP-039 | Validate production-copy safety for app, East Tech guide, and generated public output. | `deployment-qa` | `scripts/check-production-surfaces.mjs` checks exact Student/Teacher guide toggle markers, canonical workspace files, and product neutrality; `scripts/inventory-production-routes.mjs` classifies the protected workspace route and retired option roots; `scripts/check-generated-output-drift.mjs`; focused public-surface/source tests; rebuilt `public-companion/`. | Aggregate checks fail on forbidden production-surface copy, normal-nav links to alpha/account/internal smoke, public-companion alpha/account/API leaks, stale generated output, missing route inventory classification, missing guide-toggle source markers, and retired option scripts returning. | `foundation started` |
| MVP-040 | Retire stakeholder options so they cannot be confused with Student/Teacher guide modes. | `requirements-audit` | `docs/stakeholder-option-lifecycle.md`, registry classifications, package script removals, and `scripts/check-site-options.mjs` retirement validation. | Titan Blend and Back To Basics are retired as active options; Student/Teacher guide mode is implemented in the canonical East Tech guide, not `stakeholder-options/**`; Titan direction is absorbed into guide styling. | `foundation started` |

## Tenant, SSO, And Storage Direction Requirements

These requirements record the tenant-aware SaaS direction now being added to the production workspace. They do not make the app pilot-ready by themselves; they define the schema, route, docs, and test evidence needed before live school SSO or real tenant storage is accepted.

| ID | Requirement | Owner category | Current evidence | Acceptance evidence needed | Status |
| --- | --- | --- | --- | --- | --- |
| MVP-041 | Model school tenants and subscription state. | `backend-security-data` | Additive migration `migrations/0010_tenant_google_sso.sql` creates `tenants`, `tenant_domains`, `identity_providers`, `tenant_users`, `auth_identities`, and `oauth_states`, with a safe sandbox tenant/domain seed only. | Remote and local migrations are applied through Wrangler; tenant status/subscription/domain state is used by auth/admin routes without destructive changes to existing global email uniqueness. | `foundation started` |
| MVP-042 | Add Google Workspace SSO identity provider support. | `backend-security-data` | `/api/auth/config`, `/api/auth/google/start`, `/api/auth/google/callback`, `functions/_lib/auth-config.ts`, `functions/_lib/oauth-state.ts`, and `functions/_lib/google-oauth.ts` scaffold the backend OpenID Connect flow behind env flags. | Google Cloud OAuth client and Cloudflare Pages env/secrets are configured, ID-token signature/claim verification passes hosted checks, and local password login remains available for approved fallback. | `foundation started` |
| MVP-043 | Enforce tenant domain restriction through verified hosted-domain claims. | `backend-security-data` | SSO helpers validate issuer, audience, expiration, email verification, nonce, and hosted-domain policy from the ID token; docs state that the request `hd` value is only a UI hint. | Tests and hosted proof reject missing/mismatched `hd` claims for Workspace-restricted tenants and never rely on request parameters for authorization. | `foundation started` |
| MVP-044 | Link or provision identities safely. | `backend-security-data` | `auth_identities` schema and SSO callback link existing users by provider subject or existing normalized email; auto-provision creates role-pending users only when tenant policy allows it. | Bryan decides pre-provisioned-only vs role-pending auto-provision; audit proof shows denied and completed SSO attempts without OAuth codes/tokens/secrets. | `foundation started` |
| MVP-045 | Preserve local fake-account and break-glass fallback. | `deployment-qa` | `AUTH_LOCAL_LOGIN_ENABLED` defaults to enabled, `/api/auth/login` and password tests remain intact, and workspace sign-in shows local access when the safe auth config allows it. | Cloudflare env policy names who can use fallback login after SSO is live; fake `.test` login, local smoke, and admin break-glass paths keep passing. | `foundation started` |
| MVP-046 | Support school-owned evidence storage direction. | `admin-ops-reporting` | Master plan/backend docs and ADR-0002 separate Google Workspace SSO from Drive service-account storage and describe current app-managed Drive plus target tenant/school-owned storage mode. | Bryan decides app-managed vs tenant-owned Drive before real pilot storage; implementation maps tenant storage mode to repository setup/export without exposing raw Drive IDs. | `designed` |
| MVP-047 | Define tenant export and offboarding. | `admin-ops-reporting` | Master plan/backend docs and ADR-0002 require disable-access, preserve/export, school data copy, retention/audit policy, and non-destructive default offboarding. | Tenant-scoped export/offboarding routes and tests prove access disablement, data handoff, audit retention, and no destructive deletion by default. | `designed` |

## Core Requirements

| ID | Requirement | Owner category | Current evidence | Acceptance evidence needed | Status |
| --- | --- | --- | --- | --- | --- |
| MVP-001 | Build a GitHub-to-Cloudflare hosted Capstone Project app, not a static guide or Figma-only prototype. | `deployment-qa` | `docs/backend-setup.md`, `wrangler.jsonc`, Cloudflare Pages artifact, production deployment `2aadfa71`, live alpha route, `/api/alpha/state`, `docs/production-deployment-policy.md`, `docs/production-surface-registry.md`, `docs/production-predeploy-checklist.md`, deterministic route inventory in `docs/generated/production-route-inventory.md`, and P0 gate/crosswalk evidence in `docs/master-plan.md` plus `docs/source-materials/production-content-crosswalk.md`. | Verify each new post-push deployment, record URL/API status, keep canonical production deploy targets documented, designate/build the role-aware production app route, run the predeploy/P0 validation gates, and commit exact blocker if Cloudflare auth or Wrangler blocks. | `foundation started` |
| MVP-002 | Preserve the Day 7 alpha account exception while clearly labeling seeded/demo personas as not production auth. | `requirements-audit` | `docs/alpha-week-framework.md`, `docs/alpha-runbook.md`, `alpha.html`, `alpha.js`, internal QA page labels, production navigation with alpha/account links removed, and `docs/alpha-account-deployment-decision.md`. | Bryan chooses whether alpha/account stay deployed unlinked, move behind a gate, or are removed before pilot; walkthrough labels account shortcuts; no alpha copy claims pilot readiness or real account safety; production navigation stays free of alpha/smoke routes. | `alpha active` |
| MVP-003 | Exclude real student data from alpha and prevent private records from entering public/static assets. | `requirements-audit` | Alpha runbook, checker guardrails, `scripts/check-production-surfaces.mjs`, `scripts/check-generated-output-drift.mjs`, generated output source-of-truth policy, public companion output without internal alpha/account/API redirects, and the P0 forbidden production-copy list in `docs/master-plan.md`. | Contract check plus audit review confirms no real student names, private evidence, credentials, staff-only notes, fake-account UX, internal QA links, raw storage IDs, prompt/builder copy, or public-companion API redirects on production-classified public surfaces. | `foundation started` |
| MVP-004 | Provide hardened username/password pilot auth behind a narrow boundary that can later swap to SSO. | `backend-security-data` | `functions/api/auth/*`, `migrations/0001_foundation.sql`, auth setup notes, verified first-admin bootstrap for `bryan.timm89@gmail.com`, setup-key removal, login/session proof, `bootstrap_admin_created` audit event, fake `.test` role accounts including admin, auth edge-state integration coverage in `tests/auth-login.integration.test.mjs` (logout, expired-session `/me`, disabled-account `/me`, reset-required `/me`, reset-required login), and `workspace.html`/`workspace.js` now offer the canonical sign-in, password-reset completion, self-service password change, session restore, account-state blockers, role-aware dashboard load, admin import UI, and sign-out flow through `/api/auth/login`, `/api/auth/complete-reset`, `/api/auth/change-password`, `/api/auth/me`, `/api/admin/users/import`, and `/api/auth/logout`. `/api/auth/complete-reset` requires the current password for reset-required accounts, rejects weak/unchanged replacements, clears the reset state, revokes stale sessions, creates a fresh session, and audits `password_reset_completed`. `/api/admin/users/:id/require-password-reset` now lets admins require a reset for another active/reset-pending account with a reason, revokes the target user's active sessions, rejects self-reset/disabled/credentialless targets, and audits success/denials. `/api/auth/change-password` lets active signed-in users rotate known credentials, rejects weak/unchanged/current-password failures, revokes other active sessions by issuing a fresh session, and audits success/denials. `/api/admin/users/import` now lets admins import up to 25 users with a required reason, validates duplicate emails and initial role/scope records, creates `pending_reset` accounts with generated one-time temporary credentials and `requires_reset=1`, assigns the initial role, blocks real non-`.test` admin-visible temporary credential imports by default pending `HD-2026-05-21-001`, audits blocked/denied/success paths without writing temporary credentials to audit metadata, and audits authenticated non-admin denials as `admin_users_import_denied`. The canonical workspace now renders an admin-only Users tab that posts to `/api/admin/users/import`, handles role/scope validation/policy states, and displays returned one-time setup passwords only from the no-store response; hosted marker proof passed on 2026-05-21. `tests/admin-users-import.integration.test.mjs` proves imported users are blocked at normal login with `password_reset_required`, complete reset through `/api/auth/complete-reset`, then sign in normally with the new password while temporary and replacement password values stay out of audit metadata; it also proves real non-`.test` imports are blocked by default and allowed only with the explicit override. `tests/workspace-browser-smoke.test.mjs` now proves the same path against local Pages/D1 with fake `.test` accounts: admin validation failure, misc-admin denied import, no-store import response, reset-required first login without a session, reset completion, old setup credential rejection, new-password login, and admin audit readback without credential leakage. | Bryan must decide invitation, email delivery, or approved manual distribution before real pilot users; keep the default real-user import block until then, add hosted visual proof if needed, and broaden role-scope tests before pilot use. | `foundation started` |
| MVP-005 | Store users, credentials, sessions, login attempts, roles, and role assignments in D1. | `backend-security-data` | D1 migration creates auth/account tables; production D1 currently has 1 real admin plus 4 fake `.test` role accounts and `/api/health` reports `userCount=5`; `tests/auth-login.integration.test.mjs` covers active/disabled/reset-required/invalid login plus expired-session, disabled-account, reset-required `/api/auth/me`, logout revocation, reset-required credential rotation, active-user self credential rotation, `password_version` increment, stale-session revocation, fresh-session issuance, and new-password login. `tests/admin-password-reset.integration.test.mjs` covers admin-initiated reset-required flags, pending-reset account status, target session revocation, and denial handling for non-admin, disabled, credentialless, and self-reset cases. `tests/admin-users-import.integration.test.mjs` covers admin-only user import into `user_accounts`, `password_credentials`, and `user_roles`, including generated reset-required credentials, duplicate email blocking, role/scope validation, denied-role audit, no-store response proof, reset-first login enforcement, post-reset normal login, and redacted audit metadata. | D1 read/write tests cover active, disabled, reset-required, invalid login, session expiry, reset-required credential rotation, active-user credential rotation, admin-initiated reset state/session revocation, and admin user import/provisioning. | `foundation started` |
| MVP-006 | Support student, mentor, program teacher, admin, and misc admin roles with default-deny permissions. | `backend-security-data` | Migration seeds 5 roles; alpha persona switcher models scopes; fake `.test` student, mentor, program-teacher, admin, and misc-admin accounts are represented in the seed endpoint and smoke checklist; account smoke verifies selected account/session/expected role scope; executable permission-helper tests in `tests/permissions-access.test.mjs` cover the default-deny student access matrix and require both the `mentor` role and an active mentor assignment for mentor-scoped access; teacher scope checks default-deny empty `program_teacher` scope IDs (no null/empty group matching); admin provisioning route `/api/admin/mentor-assignments` requires admin and blocks creating mentor assignments unless the mentor has the `mentor` role and the student has the `student` role (audited), with integration coverage in `tests/admin-mentor-assignments.integration.test.mjs`. `tests/student-dashboard-access.integration.test.mjs` now executes `/api/student/dashboard` through the same role matrix for student-own, student-other denial, active/inactive mentor assignment, matching/empty program-teacher scope, admin inspection, and misc-admin broad-access denial, with audit assertions. `/api/teacher/review-queue` now matches the shared default-deny scope posture by rejecting empty program/cohort scope matches in SQL and auditing unauthorized, denied, and viewed queue access; `tests/review-loop.integration.test.mjs` covers missing session, misc-admin denial, empty program-scope no-leak behavior, `/api/submissions/:id` detail readback unauthorized/denied/viewed coverage, and `/api/submissions/:id/submit` missing-session/cross-student/staff denial audits with redacted actor role scopes. | Keep expanding route-level role/scope tests beyond the dashboard, review queue, review history/decision, evidence link, submission detail, and submission submit paths into remaining workflow endpoints and live UI state proof. | `foundation started` |
| MVP-007 | Let admins manage users, groups, programs, cohorts, mentor assignments, teacher assignments, and role scopes. | `admin-ops-reporting` | Figma node `48:2`; D1 tables exist; admin test account seeding endpoint `functions/api/admin/test-accounts.ts` and `tests/test-account-seed.test.mjs` exist; admin mentor assignment endpoints `/api/admin/mentor-assignments` now support list (GET), upsert/activate (POST), and deactivate (DELETE, audited) with deterministic integration coverage in `tests/admin-mentor-assignments.integration.test.mjs`; admin role-assignment endpoint `/api/admin/role-assignments` now supports list (GET), create/upsert (POST), and remove (DELETE, audited) for `user_roles` (including validated `program_teacher` program/cohort scopes) with integration coverage in `tests/admin-role-assignments.integration.test.mjs`; admin user import endpoint `/api/admin/users/import` creates pending-reset accounts, generated one-time temporary credentials, and initial role assignments with duplicate/scope validation, authenticated non-admin denial audit, reset-first login proof, and redacted audit coverage in `tests/admin-users-import.integration.test.mjs`. The canonical workspace now has an admin-only import form with reason, role/scope fields, no-store response marker, one-time setup password display, focused VM coverage, and hosted marker proof. | Broaden admin UI surfaces for role assignment, mentor assignment, import batch depth, credential delivery policy, audit role changes, and misc-admin broad-access denial. | `foundation started` |
| MVP-008 | Represent all nine required CTE programs as first-class records and filters. | `backend-security-data` | Migration seeds all nine programs. | UI/API filters and dashboard aggregates can scope by every required program. | `foundation started` |
| MVP-009 | Convert source-PDF workflow into app-native requirements, sections, quality checks, deadlines, review gates, and dashboard signals. | `requirements-audit` | `data/capstone-framework.json`; schema migration `migrations/0002_framework_seed.sql`; generated seed migration `migrations/0003_framework_seed_data.sql`; seed generator `scripts/framework-seed.mjs`; contract tests `tests/framework-seed.test.mjs` + `tests/framework-seed-migration.test.mjs`; admin test-account seeding now references seeded requirement ID `req-proposal-draft` (replacing `req-alpha-proposal`); D1 local migration + seed verification script `scripts/verify-framework-seed-d1.ps1` (local counts verified: requirements=16, deadlines=24, review_gates=4, quality_checks=20); backlog `SC-001`. On 2026-05-21 PT, remote D1 migrations `0001` through `0009` were applied/recorded through Wrangler and no pending migrations remained. | Optionally rerun remote seeded count verification after the next test-account reseed; keep future framework migrations in Wrangler's migration table. | `foundation started` |
| MVP-010 | Track student progress records and status history from trusted server/database state. | `student-workflow-evidence` | D1 tables; Figma node `61:2`; alpha state transitions; `/api/submissions/:id`, `/api/submissions/:id/submit`, `/api/submissions/:id/evidence`, `/api/reviews/:submissionId/decision`, and `/api/reviews/:submissionId/history` update/read submissions, progress records, status history, evidence metadata, and audit events. Detail/submit-route coverage now proves unauthorized, denied, allowed readback, missing-evidence, and successful submit audits, including role-scope metadata and immutable submission-version snapshots. | Add stale-write handling and broader route tests; dashboard counts derive from DB/server state. | `foundation started` |
| MVP-011 | Provide student dashboard with phase, next action, due items, evidence needs, revision state, and program context. | `student-workflow-evidence` | Alpha student workspace, server-derived next-step guidance, reviewer checks, copyable walkthrough summary, Figma student desktop/mobile frames, and D1-backed `/api/student/dashboard`. The dashboard route now audits unauthorized, denied, and allowed protected-record views and has integration coverage proving returned progress/submission/evidence summaries omit Drive storage identifiers. | Mobile and desktop walkthrough shows live state changes after submit/revision/approval. | `alpha active` |
| MVP-012 | Provide guided proposal/research workflow with sections, evidence requirements, draft, submit, revise, and resubmit. | `student-workflow-evidence` | Alpha actions and Figma guided proposal `3:154`; alpha state machine now blocks out-of-order submit/resubmit/approve/revision transitions and duplicate meeting/export actions; production `/api/submissions/:id` now returns scoped submission detail/evidence metadata readback with missing-session, denied, and viewed audits, while `/api/submissions/:id/submit` blocks submission when no evidence artifacts are attached (409 `submission_missing_evidence`) and audits missing-session attempts, cross-student/staff denials, blocked missing-evidence submits, and successful submits with redacted actor role scopes. | Server-owned guided sections persist drafts/submissions and expose blocked-submit reasons before students submit; remaining follow-up is stale-write handling and UI consumption of the protected detail route. | `alpha active` |
| MVP-013 | Validate evidence metadata and support private evidence links/uploads through Google Drive repository path. | `student-workflow-evidence` | D1 `evidence_artifacts`, Drive root/index artifacts, alpha evidence URL validation, health readiness fields, and `/api/submissions/:id/evidence` for scoped HTTPS evidence-link metadata attachment; that route now audits missing-session attempts, denied cross-student/staff attempts, and successful link attachment with redacted actor role scopes, with `tests/evidence-link.integration.test.mjs` covering 401/403/200 behavior and no Drive storage-ID exposure. Service-account JWT Drive token exchange helper (`functions/_lib/google-drive.ts`) and authenticated/audited `/api/evidence/drive-probe` route have integration tests covering missing config/creds, token exchange failure, Drive access-denied responses, and redacted root/index statuses. Drive-backed evidence file-byte upload route `/api/submissions/:id/evidence/upload` now supports multipart uploads (≤5MB) and resumable uploads (>5MB, capped at 20MB) -> Drive -> writes `evidence_artifacts.drive_file_id`, with integration tests covering missing session, missing submission, student-other-submission denial, non-student denial, unsupported/empty/oversized files before provider calls, missing Drive config/credentials, token failure, provider exception, provider forbidden/upload failure, successful redacted metadata, and the resumable path. `/api/student/dashboard` now returns storage-ID-redacted evidence summaries with safe `downloadUrl` values for Drive-backed evidence and `externalUrl` values for external links, and the canonical workspace renders `data-evidence-download="file"` / `data-evidence-link="external"` actions without raw Drive IDs. `scripts/check-google-drive-live.mjs` and `npm run check:drive:live` now provide a first-class live Drive gate. On 2026-05-21, Bryan provided Shared Drive root `0AJHkstxfN-dTUk9PVA`; `wrangler.jsonc`, Cloudflare Pages production/preview env vars, and remote D1 metadata were updated; production Pages was redeployed; and `npm run check:drive:live` passed provider config, fake `.test` auth, denial guards, Drive token/root/index probes, hosted workspace marker proof, fake text upload, app-scoped download byte comparison, D1 upload/download audit verification, dashboard leak checks, and a >5MB resumable fake `.test` upload/download proof. Access-controlled streaming download route `/api/evidence/:id/download` has integration tests that stub fetch() for token exchange/provider failures and assert audit events + no storage-id leaks. | Add browser-level upload progress/retry proof, Google Docs export cases, and broader permission-state UI coverage before accepting real student uploads. | `foundation started` |
| MVP-014 | Keep evidence artifacts access-controlled and auditable, including denied-access events. | `backend-security-data` | Figma nodes `37:2`, `43:149`, `56:2`; D1 evidence tables; `/api/evidence/:id/check-access` checks current session scope with `canAccessStudent`, audits unauthorized/denied/allowed/missing access, and withholds Drive storage IDs; `tests/permissions-access.test.mjs` exercises `canAccessStudent` across the role matrix used by evidence access checks (including empty scope-id denial); `tests/evidence-check-access.integration.test.mjs` executes the evidence access route (401/403/404/200) and asserts audit-event writes plus storage-id redaction (including empty program-teacher scope-id denial); `tests/evidence-link.integration.test.mjs` now proves `/api/submissions/:id/evidence` audits missing session, cross-student denial, non-student role denial, and successful external-link attachment with actor role scopes and no Drive storage identifiers; `tests/evidence-drive-file.integration.test.mjs` covers `/api/submissions/:id/evidence/upload` denial/no-insert behavior plus `/api/evidence/:id/download` (401/403/404/409/502/200) including student/mentor/program-teacher/admin allow-path streaming, misc-admin denial, and role-scope audit assertions. `/api/student/dashboard` and `/api/submissions/:id` now have protected-record route coverage that verifies no Drive storage ID leaks from evidence summaries/readback while access is audited. Live `npm run check:drive:live` now proves hosted fake `.test` text upload/download, >5MB resumable upload/download, remote D1 upload/download audit rows, denial guards, dashboard download URL redaction, and no raw Drive storage IDs in browser/API output. | Browser/UI tests still need to prove hosted permission-denied, role-pending, no-assignment, and upload progress/retry states clearly for each role. | `foundation started` |
| MVP-015 | Provide program teacher review queue, detail, comments, revision request, approval, status history, and dashboard count updates. | `staff-review-mentor` | Alpha teacher review actions; Figma nodes `31:2`, `61:2`; `/api/teacher/review-queue`, `/api/submissions/:id`, `/api/reviews/:submissionId/decision`, and `/api/reviews/:submissionId/history` persist/list review work with teacher/admin scope checks; `/api/teacher/review-queue` now audits `review_queue_unauthorized`, `review_queue_denied`, and `review_queue_viewed`, includes redacted actor role scopes, and default-denies empty program/cohort scope IDs instead of matching null/empty group values; `/api/submissions/:id` now audits submission detail unauthorized/denied/viewed access and returns storage-ID-redacted evidence metadata for scoped student/mentor/teacher/admin readback; `/api/reviews/:submissionId/decision` now supports `comment_only` review records that persist into `comments` without changing submission status, audits missing-session and denied reviewer attempts as `review_decision_unauthorized` and `review_decision_denied`, and includes redacted actor role scopes in denial/success audit metadata; `/api/reviews/:submissionId/history` now audits missing-session, denied, and successful history reads as `review_history_unauthorized`, `review_history_denied`, and `review_history_viewed` with redacted role scopes and result counts. Integration test `tests/review-loop.integration.test.mjs` executes student submit -> teacher comment -> teacher revision -> student resubmit (version++) -> teacher approval and asserts queue membership + persisted `reviews` + `comments` + `status_history`; it also covers review-queue missing-session audit, misc-admin denial audit, empty program-scope no-leak behavior, submission-detail unauthorized/denied/viewed audit, review-history unauthorized/denied/viewed audit, review-decision unauthorized/denied audit, and submission submit unauthorized/denied/blocked/success audit paths. `account.html`/`account.js` now include a review-history smoke panel that renders comments, review decisions, status changes, and versions from `/api/reviews/:submissionId/history`; the primary alpha console now preserves and renders teacher comments, decisions, status events, and version snapshots from server-owned alpha state with a storage-identifier guard. | Confirm the hosted alpha/account walkthrough completes student -> teacher comment/revision -> resubmission -> approval without code edits, with immutable history visible in the UI and dashboards updating from DB/server state. Continue route-level permission/audit depth for mentor meetings, presentation slots, archive/export, and hosted permission-denied UI states. | `alpha active` |
| MVP-016 | Preserve immutable review history and prior submission versions. | `staff-review-mentor` | Figma node `37:2`; alpha review history timeline; D1 migration `migrations/0005_submission_versions.sql`; `/api/submissions/:id/submit` writes immutable `submission_versions` snapshots with evidence metadata on submit/resubmit; `/api/reviews/:submissionId/history` returns review decisions, comments, status history, and version snapshots and now audits unauthorized, denied, and viewed history access with redacted role-scope metadata; `tests/review-loop.integration.test.mjs` covers comments plus two preserved versions across revision/resubmission/approval and the history access audit matrix; the account smoke UI now renders returned comments and version snapshots while blocking raw Drive storage identifiers, with regression coverage in `tests/account-and-evidence-access.test.mjs`; `functions/_lib/alpha-flow-model.js` and `alpha.js` now mirror that history shape in the primary alpha walkthrough, backed by `tests/alpha-flow.test.mjs` and `scripts/check-alpha-contract.mjs`. | Confirm the hosted account/alpha UI renders version history without exposing Drive storage IDs; remote D1 migrations through `0009` are already applied/recorded, so future work should focus on hosted UI proof and remaining workflow endpoint permissions. | `foundation started` |
| MVP-017 | Support mentor assigned-student visibility, meeting attendance, make-up requirements, outline approval cues, presentation slot risk, and scoped actions. | `staff-review-mentor` | Alpha mentor actions; Figma node `78:2`; backlog `SC-004`; `/api/mentor/assigned` exposes active mentor assignments with submission status and evidence counts; meeting persistence migration `migrations/0004_mentor_meetings.sql`; `/api/mentor/meetings` now supports scoped meeting readback for student self, active assigned mentor, scoped program teacher, and admin viewers, denies misc-admin broad access plus inactive/student-other scopes, and audits `mentor_meetings_unauthorized`, `mentor_meetings_denied`, `mentor_meetings_viewed`, `mentor_meeting_unauthorized`, `mentor_meeting_denied`, and `mentor_meeting_held` with redacted actor role scopes in `tests/mentor-meetings.integration.test.mjs`; presentation slot migration `migrations/0006_presentation_slots.sql`; `/api/presentation-slots` lists only role-scoped slots, limits scheduling to admin/program-teacher staff, blocks same-location overlapping slots, audits missing-session, misc-admin denial, viewed result counts, denied/conflict/scheduled outcomes, and proves student-own/student-other plus empty-scope no-leak states; `/api/presentation-slots/:id/check-out` and `/api/presentation-slots/:id/check-in` now persist day-of status/timestamps for in-scope admin/program-teacher staff and audit missing-session, role/scope/status denials, and successful transitions; `workspace.js` now consumes `/api/presentation-slots` in the canonical workspace and renders a Presentation section with schedule, location, outline status, empty state, and staff-only check-out/check-in buttons for student, mentor, program-teacher, and admin roles. Coverage lives in `tests/presentation-slots.integration.test.mjs`, `tests/workspace-app.test.mjs`, and credential-backed `tests/workspace-browser-smoke.test.mjs`; in-app browser verified the local program-teacher Presentation tab with a scheduled slot, Room 101, one check-out button, and zero console errors. Remote migration `0006` is now applied/recorded. | Persist outline approval gates beyond the slot field if needed, add hosted presentation-dashboard proof after deploy, and continue celebration/archive plus hosted permission UI depth. | `foundation started` |
| MVP-018 | Support admin overview, deadlines, templates, overrides, audit/activity, exports, and archive controls. | `admin-ops-reporting` | Alpha admin actions; Figma nodes `48:2`, `69:2`, repo-consumed node `144:2`, provider/retention handoff node `149:2`, and Drive archive delivery handoff node `151:2`; D1 tables for deadlines, exports, audit, and deprecated/schema-only announcements; `/api/admin/exports/student-archive`, `/api/exports/:id/download`, `/api/student/archive/readiness`, and `/api/admin/audit-events` cover the first persisted admin/archive operations. Admin archive generation now requires an explicit reason, audits missing-session and non-admin attempts, verifies Drive repository/credential/provider readiness before success, records failed provider states as failed exports with `student_archive_export_provider_unavailable`, uploads the storage-ID-redacted manifest JSON into the configured Drive root before marking an export complete, stores the Drive file ID server-side in `exports.drive_file_id`, and only exposes boolean Drive-package-ready markers to clients. | Admin UI surfaces persist operations, require override reasons, redact audit details, add signed-link delivery or hosted Drive-package proof after secrets/deployment, record export lifecycle, and expose hosted provider/retention states after deployment. | `foundation started` |
| MVP-019 | Provide misc-admin narrow read/reporting view without broad admin powers. | `admin-ops-reporting` | Alpha misc-admin report and restricted approval denial; `/api/reports/readiness` is admin/misc-admin only and returns aggregate counts without names, emails, or student-level rows. | Tests show misc-admin can run allowed reports but cannot approve, export protected archives, change roles, or view out-of-scope evidence. | `alpha active` |
| MVP-020 | Maintain audit logs for sensitive actions: login, role changes, progress/status changes, evidence access, review decisions, overrides, exports, and permission denials. | `admin-ops-reporting` | D1 `audit_events`; alpha activity timeline; Figma node `69:2`; `/api/admin/audit-events` is admin-only and redacts sensitive metadata keys; mentor assignment provisioning/denials (`/api/admin/mentor-assignments`), admin password reset initiation/denials (`/api/admin/users/:id/require-password-reset`), admin user imports, real-user import policy blocks, and authenticated non-admin import denials (`/api/admin/users/import`), self-service password change success/denial (`/api/auth/change-password`), archive unauthorized/denied/provider-unavailable/generation/download/missing-artifact/expired-download checks, student archive-readiness unauthorized/views/denials, presentation-slot unauthorized/denied/viewed/conflict/scheduled/transition events, readiness report views, mentor meeting unauthorized/denied/viewed/held checks, student dashboard unauthorized/denied/viewed events, teacher review queue unauthorized/denied/viewed events, submission-detail unauthorized/denied/viewed events, review-history unauthorized/denied/viewed events, review-decision unauthorized/denied events, evidence checks, and review decisions write audit events. `tests/archive-readiness.integration.test.mjs` covers missing-session readiness/export/download audits, non-admin archive export denial, own-student view audit, out-of-scope denial audit, misc-admin denial, Drive credential/access failure audits, archive generation audit, scoped download audit, missing artifact audit, and expired package audit; `tests/presentation-slots.integration.test.mjs` covers presentation list unauthorized/denied/viewed metadata plus transition unauthorized/denied/success events; `tests/mentor-meetings.integration.test.mjs` covers `mentor_meetings_unauthorized`, `mentor_meetings_denied`, `mentor_meetings_viewed`, `mentor_meeting_unauthorized`, `mentor_meeting_denied`, and `mentor_meeting_held` metadata with role scopes; `tests/admin-password-reset.integration.test.mjs` covers reset-required success/denial audit metadata; `tests/admin-users-import.integration.test.mjs` verifies generated temporary credentials and replacement passwords are absent from import/reset audit metadata and covers `admin_users_import_denied` plus `admin_users_import_blocked`; `tests/student-dashboard-access.integration.test.mjs` covers `student_dashboard_unauthorized`, `student_dashboard_denied`, and `student_dashboard_viewed` metadata; `tests/review-loop.integration.test.mjs` covers `review_queue_unauthorized`, `review_queue_denied`, `review_queue_viewed`, `submission_detail_unauthorized`, `submission_detail_denied`, `submission_detail_viewed`, `review_history_unauthorized`, `review_history_denied`, `review_history_viewed`, `review_decision_unauthorized`, and `review_decision_denied` metadata with role scopes; `tests/workspace-browser-smoke.test.mjs` now reads `/api/admin/audit-events` after local admin import/reset proof and verifies import denial, import, and reset events without runtime credential leakage; `tests/auth-login.integration.test.mjs` covers `password_changed_by_user` and `password_change_denied`. | Audit API/view filters immutable events, redacts sensitive metadata, and records denied access. | `foundation started` |
| MVP-021 | Generate dashboard aggregates from trusted state for students, mentors, teachers, admins, and misc admins. | `student-workflow-evidence` | Alpha dashboard metrics; Figma node `61:2`; the canonical workspace now renders presentation schedule/status cards from the authenticated `/api/presentation-slots` response instead of client-only workflow state, with source, credential-backed HTTP, aggregate check, and in-app browser proof for the local program-teacher path. | Broaden DB-derived dashboard aggregates for progress/review/export counts and add hosted presentation-dashboard proof after deployment. | `alpha active` |
| MVP-022 | Support exports/archives, including May 5 student archive package and scoped signed download behavior. | `admin-ops-reporting` | D1 `exports`; Figma nodes `69:2`, repo-consumed `144:2`, provider/retention handoff `149:2`, and Drive archive delivery handoff `151:2`; alpha export queue; migration `0007_archive_export_artifacts.sql` adds `export_artifacts` and is now applied/recorded remotely. `/api/student/archive/readiness` derives Celebration Day evidence, thank-you/mentor note, reflection/portfolio, ingredient-list, export, storage-provider, scoped-package readiness, retention policy/window state, Drive-package readiness, and signed-download readiness from persisted progress/evidence/export/artifact rows with storage IDs redacted and now audits missing-session attempts in addition to views/denials. The canonical workspace now includes a student Archive tab, manifest download link when a scoped manifest exists, a Drive package file row, and retention-policy status row. `/api/admin/exports/student-archive` blocks missing-session, non-admin, missing Drive config/credentials/provider access, uploads the redacted manifest JSON to Drive only after provider checks pass, records Drive upload failures as failed exports, and stores the Drive file ID server-side while `export_artifacts` keeps the app-scoped manifest body/hash/expiry. `/api/exports/:id/download` enforces auth and scoped access, audits missing-session/denied/missing/expired/download states, streams unexpired manifests, returns expired-package retry state, and emits only `x-archive-drive-package-ready`/redaction headers, not Drive IDs. Coverage: `tests/archive-readiness.integration.test.mjs` (15 archive/export tests), `tests/workspace-app.test.mjs`, credential-backed `tests/workspace-browser-smoke.test.mjs`, source tests, and in-app browser student Archive tab proof with zero console errors. Node `149:2` is now partially consumed in code for missing Drive credentials, provider-unavailable retry, retention-window expiring, and policy review states; node `151:2` is now partially consumed by Drive-backed package-file generation; the Shared Drive live gate now passes fake upload/download and >5MB resumable proof. | Capture hosted fake-account proof for Drive package upload/download markers, decide whether signed Drive links are needed beyond scoped app downloads, and confirm school retention policy. | `foundation started` |
| MVP-023 | Removed from MVP: announcement surfaces for staff/admin updates. | `admin-ops-reporting` | Announcement routes and workspace announcement cards were removed from active MVP product surfaces in Phase 3. D1 `announcements` remains deprecated/schema-only for later safe cleanup. Schools should continue using existing communication systems. | No active announcement creation/list/read product surface remains in the sales MVP. | `removed from MVP` |
| MVP-024 | Preserve mobile-safe student path with no horizontal overflow. | `student-workflow-evidence` | Alpha CSS and earlier Edge mobile render proof; Figma node `56:2`. | Browser/mobile smoke proof after current alpha route, including revision/evidence states. | `foundation started` |
| MVP-025 | Maintain CI and local validation for alpha contract, state machine, syntax, type checks, and future permission tests. | `deployment-qa` | `npm run check`, `scripts/check-alpha-contract.mjs`, `scripts/check-production-surfaces.mjs`, `scripts/inventory-production-routes.mjs`, `scripts/check-generated-output-drift.mjs`, `tests/alpha-flow.test.mjs`, `tests/test-account-seed.test.mjs`, `.github/workflows/*.yml`; `tests/production-workflow-source.test.mjs` now asserts the generated route inventory classifies `workspace.html` as `senior-capstone-app` production/conditional rather than `unknown` and locks the protected submission-detail route markers; `tests/review-loop.integration.test.mjs` now includes teacher review queue audit, empty program-scope no-leak, submission-detail access audit, and storage-ID redaction regressions. | CI green on GitHub, production-surface leak checks pass, route inventory is current, generated output drift checks pass, P0 production-copy/nav/toggle-pending signals are explicit, and local/Node REPL fallback is documented when local shell lacks Node/npm/Wrangler. | `foundation started` |
| MVP-026 | Verify Cloudflare Pages deployment, D1 binding, env vars/secrets, and `/api/alpha/state` after pushes. | `deployment-qa` | Production deployment `2aadfa71` verified `/alpha.html`, `/api/alpha/state`, `/api/health`, D1 first-admin state, login/session, and bootstrap 403 after setup-key removal. Production deployment `c7908d04` for commit `dc2f82a` verified the admin-only test-account seed endpoint; `/api/health` now reports `userCount=5`, all four fake role accounts log in, unauthenticated seed calls return 401, and student seed calls return 403. Health route now reports evidence root/index and Drive credential readiness booleans for smoke checks. `docs/production-predeploy-checklist.md` now defines static/live Cloudflare gates plus `check:drive:live`. On 2026-05-20, static Cloudflare checks passed, direct live HTTPS checks verified `/workspace`, `/api/health`, `/api/auth/me` signed-out, signed-out logout, and fake student login/session/dashboard/evidence-link/upload-block/logout. On 2026-05-20 19:10 PT, direct hosted `workspace.js` returned HTTP 200 and contained the new `/api/presentation-slots` and `data-presentation-action="check-out"` markers after the workspace presentation-dashboard push. On 2026-05-20 21:43 PT, direct hosted `workspace.js` returned HTTP 200 and contained `data-archive-retention-status` after the archive provider/retention push. On 2026-05-20 22:43 PT, aggregate `check` passed local/static gates but live Cloudflare verification failed because the exported token was rejected as `Invalid access token [code: 9109]`; that is historical. On 2026-05-20 23:11 PT, `check:cloudflare` and `check:cloudflare:live` passed with a user-scope token. On 2026-05-21 PT, remote D1 migrations `0001` through `0009` were applied/verified, a production Pages deploy completed from the Shared Drive root update, and after commit `d0fe1f6` deployed, hosted `workspace.js` returned `data-evidence-download="file"` on poll 3 while `npm run check:drive:live` passed fake upload/download, >5MB resumable upload/download, metadata/audit, denial, and leak checks. | After each commit lands, verify hosted route/API/asset markers or record the exact connector/Wrangler blocker; keep `check:cloudflare:live` and `check:drive:live` passing before accepting real student uploads. | `foundation started` |
| MVP-027 | Maintain backup/export posture, retention notes, secrets discipline, and no credential commits. | `deployment-qa` | `.gitignore`, backend setup notes, artifact registry, alpha runbook, `docs/production-predeploy-checklist.md`, `docs/custom-domain-cutover-checklist.md`, `.dev.vars.example`/`wrangler.jsonc` archive retention knobs, and local smoke credential workflow that writes passwords only to ignored `.secrets/local-workspace-smoke-accounts.json`. | Backup/restore, rollback, custom-domain cutover, and secret rotation checklists exist; scan confirms no secrets or real records committed. | `foundation started` |
| MVP-028 | Keep Figma as functional product-design source for route/data/permission/states, not production data. | `design-assets-handoff` | Owned by Functionality UX Upgrade when a bounded design handoff is needed. Active file `z4t4tFPAKrMDh6pIYOeEw6`, nodes `18:2`, `31:2`, `37:2`, `43:2`, `48:2`, `56:2`, `61:2`, `69:2`, `78:2`, and prototype page `98:2`; 2026-05-20 alignment updated and verified review-history prototype nodes `98:9` and `98:10`; node `98:17` records primary alpha-console review-history consumption; node `124:2` distinguishes public guide mode from authenticated `/workspace`; node `133:2` maps workspace account edge states; node `139:2` maps presentation dashboard states; node `144:2` maps celebration/archive readiness; node `149:2` maps archive provider failures, remote migration pending setup, signed/scoped download expiry, and retention-policy review states; node `151:2` maps Drive-backed archive package/signed-link delivery and hosted-proof states to export/evidence/audit routes and records; node `153:2` maps credential lifecycle states for admin reset initiation, reset completion, active-user password change, invalid current password, weak/reused password, stale-session revocation, and redacted audit proof; node `158:2` maps admin import batch, one-time temporary credential display, delivery-policy-needed, validation, reset-required, and denied-audit states; node `163:2` maps admin import proof QA for hosted form markers, validation blocking, no-store setup output, reset-first login, denied-role behavior, stale-session safety, audit redaction, and now records consumed API proof plus remaining browser proof checks; node `173:2` maps student dashboard access audit states; node `178:2` maps teacher review queue access audit and empty-scope no-leak states; node `180:2` maps review history and decision access audit states; node `183:2` maps evidence-link metadata attach access audit states; node `187:2` maps submission-submit access audit states; node `190:2` maps submission-detail readback access audit states. | Functionality UX Upgrade runs may produce or consume Figma handoffs when they support route/data/permission annotations, state variants, screenshots/metadata, or exact blockers. No broad Figma polish while Day 7 alpha gaps are open unless a specific implementation ambiguity exists. | `designed` |
| MVP-029 | Keep Canva as supporting-image source with live-text and privacy discipline. | `design-assets-handoff` | Canva folder/artifacts and open empty-state handoffs. Canva support is owned by Functionality UX Upgrade only when it directly improves a safe product-readiness slice. | Asset specs include placement, dimensions, alt text, no private data, and live-text guidance; assets are used only where helpful. Figma usage may document Canva placement/relationship to a design handoff, not perform unrelated Canva work. | `designed` |
| MVP-030 | Keep the 100-pass target honest with evidence-based calibration. | `requirements-audit` | Current single-automation cadence docs, Functionality UX Upgrade prompt, growth ledger/state, the production-surface 100-pass report, the predeploy-gate follow-up batch, and the P0 production app/website gate. | Functionality UX Upgrade accepted passes require durable implementation, design handoff, validation, artifact, exact blocker evidence, or production-surface/deploy-gate validation that removes deploy ambiguity. Alpha/smoke/preview/stakeholder-only work does not count as production-experience completion. | `foundation started` |

### MVP-028 Evidence Addendum

- 2026-05-20 20:18 PT: Figma node `144:2` (`Prototype / 18 / Celebration archive readiness handoff`) now maps Celebration Day evidence, ingredient-list requirements, thank-you/mentor note completion, reflection/portfolio readiness, May 5 archive request, expired signed download, and archive permission-denied states to `/student/celebration`, `/student/portfolio`, `/student/archive`, `/api/submissions/:id/evidence`, `/api/evidence/:id/check-access`, `/api/exports/student-archive`, `/api/exports/:id/download`, and `/admin/audit`. Shared plugin data key `senior_capstone/celebration_archive_readiness_contract_2026_05_20` records 7 states, 8 routes, 14 records, 5 permission scopes, 7 guardrails, and 7 acceptance checks. `get_design_context`, `get_screenshot`, and final readback succeeded with zero suspicious clipped text nodes and zero child overflow after row autosizing correction.
- 2026-05-20 21:17 PT: Figma node `149:2` (`Prototype / 19 / Archive provider and retention handoff`) now maps missing Drive credentials, provider-unavailable retry, queued export generation, scoped package readiness, retention-window expiry, and policy-review states to `/api/admin/exports/student-archive`, `/api/exports/:id/download`, `/api/student/archive/readiness`, `/api/evidence/drive-probe`, `/api/submissions/:id/evidence/upload`, and `/admin/audit`. Shared plugin data key `senior_capstone/archive_provider_retention_contract_2026_05_20` records 6 states, 6 routes, 10 records, 5 permission scopes, 6 guardrails, 7 acceptance checks, and the current Cloudflare/Drive blockers. `use_figma`, `get_design_context`, and `get_screenshot` succeeded with zero suspicious clipped text nodes and zero child overflow; screenshot returned `706x1024` from original `1360x1975`.
- 2026-05-20 21:48 PT: Figma node `151:2` (`Prototype / 20 / Drive archive delivery handoff`) now maps Drive credentials configured, package assembly to Drive, signed link issued, student download started, expired link retry, and hosted proof required states to `/api/admin/exports/student-archive`, `/api/exports/:id/download`, `/api/student/archive/readiness`, `/api/evidence/drive-probe`, and `/admin/audit`. Shared plugin data key `senior_capstone/drive_archive_delivery_contract_2026_05_20` records 6 states, 5 routes, 10 records, 5 permission scopes, 6 guardrails, and 6 acceptance checks. `use_figma`, `get_design_context`, and `get_screenshot` succeeded with zero suspicious clipped text nodes and zero child overflow; screenshot returned `684x1024` from original `1360x2038`.
- 2026-05-21 00:22 PT: Figma node `153:2` (`Prototype / 21 / Credential lifecycle handoff`) now maps admin reset requested, reset completion form, password changed plus session rotation, invalid current password, weak or reused password, and stale session revoked states to `/workspace`, `/api/auth/login`, `/api/auth/me`, `/api/auth/complete-reset`, `/api/auth/change-password`, `/api/admin/users/:id/require-password-reset`, and `/admin/audit`. Shared plugin data key `senior_capstone/credential_lifecycle_contract_2026_05_21` records 6 states, 7 routes, 5 records, and 6 guardrails. `use_figma`, `get_design_context`, and `get_screenshot` succeeded after a compact chip and state-row overflow correction; final readback found 59 text nodes, zero suspicious clipped text nodes, and zero child overflow; screenshot returned `868x1024` from original `1360x1605`.
- 2026-05-21 00:54 PT: Figma node `158:2` (`Prototype / 22 / Admin import temporary credential handoff`) now maps import batch draft, validation errors, pending-reset account creation, one-time temporary credential display, delivery policy needed, and permission-denied audit states to `/admin/users`, `/api/admin/users/import`, `/api/admin/role-assignments`, `/api/admin/users/:id/require-password-reset`, `/api/auth/complete-reset`, `/workspace`, and `/admin/audit`. Shared plugin data key `senior_capstone/admin_import_temp_credential_contract_2026_05_21` records 6 states, 7 routes, 9 records, 4 permission scopes, 6 guardrails, and 5 acceptance checks. `use_figma`, `get_design_context`, and `get_screenshot` succeeded after text-height and chip/row corrections; final readback found 59 text nodes, zero suspicious clipped text nodes, and zero child overflow; screenshot returned `814x1024` from original `1360x1712`.
- 2026-05-21 01:23 PT: Figma node `163:2` (`Prototype / 23 / Admin import proof QA handoff`) now maps hosted admin form loaded, validation errors blocked, import success no-store, reset-first login required, denied role attempt, and refresh/stale-session safety states to `/workspace`, `/api/auth/me`, `/api/admin/users/import`, `/api/auth/login`, `/api/auth/complete-reset`, `/api/auth/change-password`, `/api/admin/audit-events`, and `/admin/audit`. Shared plugin data key `senior_capstone/admin_import_proof_qa_contract_2026_05_21` records 6 states, 8 routes, 9 records, 5 UI markers, 6 guardrails, and 5 acceptance checks. `use_figma`, `get_design_context`, and `get_screenshot` succeeded after text-height correction; final readback found 49 text nodes, zero suspicious clipped text nodes, and zero child overflow; screenshot returned `985x1024` from original `1360x1414`.
- 2026-05-21 01:49 PT: Figma node `163:2` was updated after reset-first API proof landed in the non-Figma lane. Shared plugin data key `senior_capstone/admin_import_proof_qa_contract_2026_05_21` now records `apiProofConsumedAt=2026-05-21T01:37:00-07:00`, 7 consumed API proof checks, 7 remaining browser proof checks, and commit `f9f6ea8db24b064a821571c4b731236ed386b5f4`. `use_figma` updated 11 text nodes and read back 49 text nodes, zero collapsed text nodes, and zero child overflow. `get_design_context` and `get_screenshot` succeeded; screenshot returned `1007x1024` from original `1360x1384`.
- 2026-05-21 02:50 PT: Figma node `124:2` (`Prototype / 15 / Production app and public guide boundary`) was updated after generated route inventory classified `workspace.html` as `senior-capstone-app` / `production` / `conditional`. Shared plugin data key `senior_capstone/production_route_inventory_consumption_2026_05_21` records consumed commit `b318eb146c8be4aac41fec3cca2adc817a01ec91`, consumed manifest `docs/progress/runs/2026-05-21-0233-workspace-route-inventory-mvp-032.json`, three consumed route-inventory proof points, and four remaining implementation proof items. Final readback found 79 text nodes, zero collapsed text nodes, zero suspicious clipped text nodes, and zero direct-child overflow after a footer-width correction. `get_design_context` and `get_screenshot` succeeded; screenshot returned `783x1024` from original `1360x1779`.
- 2026-05-21 03:24 PT: Figma node `173:2` (`Prototype / 24 / Student dashboard access audit handoff`) was created after `/api/student/dashboard` gained route-level protected-record audit proof. Shared plugin data key `senior_capstone/student_dashboard_access_audit_contract_2026_05_21` records consumed commit `d8eb8c95b56406c0c8c051ea0d55876986112567`, consumed manifest `docs/progress/runs/2026-05-21-0306-student-dashboard-access-audit-mvp-006.json`, routes `/api/student/dashboard`, `/api/auth/me`, `/workspace`, `/admin/audit`, and `/api/audit-events`, audit events `student_dashboard_unauthorized`, `student_dashboard_denied`, and `student_dashboard_viewed`, 5 permission scopes, 5 guardrails, and 4 acceptance checks. Final readback found 49 text nodes, zero suspicious clipped text nodes, and zero child overflow after row and text-height corrections. `get_design_context` and `get_screenshot` succeeded; screenshot returned `958x1024` from original `1360x1455`.
- 2026-05-21 03:52 PT: Figma node `178:2` (`Prototype / 25 / Teacher review queue access audit handoff`) was created after `/api/teacher/review-queue` gained unauthorized, denied, viewed, and empty-scope no-leak proof. Shared plugin data key `senior_capstone/teacher_review_queue_scope_audit_contract_2026_05_21` records consumed run `2026-05-21-0336-teacher-review-queue-scope-audit-mvp-015`, consumed commits `0920bf2d33af753817700439bf44374655c57958` and `2e0ac3262c9252bd1d98358ac97d6c420ab30df9`, routes `/api/teacher/review-queue`, `/teacher/reviews`, `/workspace`, `/admin/audit`, `/api/audit-events`, `/api/reviews/:submissionId/history`, and `/api/reviews/:submissionId/decision`, audit events `review_queue_unauthorized`, `review_queue_denied`, and `review_queue_viewed`, 6 states, 5 permission scopes, 5 guardrails, and 4 acceptance checks. Final readback found 49 text nodes, zero suspicious clipped text nodes, zero direct-child overflow, and zero collapsed frames. `get_design_context` and `get_screenshot` succeeded; screenshot returned `1010x1024` from original `1360x1379`.
- 2026-05-21 04:22 PT: Figma node `180:2` (`Prototype / 26 / Review history and decision access audit handoff`) was created after `/api/reviews/:submissionId/history` gained unauthorized/denied/viewed audits and `/api/reviews/:submissionId/decision` gained unauthorized/denied reviewer audits. Shared plugin data key `senior_capstone/review_history_decision_access_audit_contract_2026_05_21` records consumed run `2026-05-21-0405-review-history-decision-audit-mvp-015`, consumed commits `d083100cd9cc501643217d627948026be4753f24` and `737fbc1`, routes `/api/reviews/:submissionId/history`, `/api/reviews/:submissionId/decision`, `/api/teacher/review-queue`, `/workspace`, `/admin/audit`, and `/api/audit-events`, audit events `review_history_unauthorized`, `review_history_denied`, `review_history_viewed`, `review_decision_unauthorized`, `review_decision_denied`, `submission_approved`, `submission_revision_requested`, and `submission_review_comment_added`, 6 states, 5 guardrails, and 4 acceptance checks. Final readback found 50 text nodes, zero suspicious clipped text nodes, and zero child overflow after text-height and auto-layout correction. `get_design_context` and `get_screenshot` succeeded; screenshot returned `952x1024` from original `1360x1463`.
- 2026-05-21 04:54 PT: Figma node `183:2` (`Prototype / 27 / Evidence link access audit handoff`) was created after `/api/submissions/:id/evidence` gained missing-session, denied, and successful evidence-link metadata attach audits. Shared plugin data key `senior_capstone/evidence_link_access_audit_contract_2026_05_21` records consumed run `2026-05-21-0439-evidence-link-access-audit-mvp-013`, consumed commits `7dd04d67039c0473a62d9a95b3ff05298bb36c72` and `29146c3e3b148a67237a0215901c4572c809ba67`, routes `/api/submissions/:id/evidence`, `/student/evidence`, `/workspace`, `/api/evidence/:id/check-access`, `/api/submissions/:id/evidence/upload`, `/api/evidence/:id/download`, and `/admin/audit`, audit events `evidence_attach_unauthorized`, `evidence_attach_denied`, and `evidence_link_attached`, 6 states, 7 routes, 10 records, 5 permission scopes, 6 guardrails, and 5 acceptance checks. Final readback found 50 text nodes, zero suspicious clipped text nodes, and zero child overflow after text-height correction. `get_design_context` and `get_screenshot` succeeded; screenshot returned `972x1024` from original `1360x1434`.
- 2026-05-21 05:30 PT: Figma node `187:2` (`Prototype / 28 / Submission submit access audit handoff`) was created after `/api/submissions/:id/submit` gained missing-session, denied, missing-evidence blocked, and successful submit audits. Shared plugin data key `senior_capstone/submission_submit_access_audit_contract_2026_05_21` records consumed run `2026-05-21-0506-submission-submit-access-audit-mvp-012`, consumed commit `c35dd306c199f29d3e5d64a802c6912ca4de13c0`, routes `/api/submissions/:id/submit`, `/student/submissions/:id`, `/workspace`, `/api/reviews/:submissionId/history`, `/admin/audit`, and `/api/audit-events`, audit events `submission_submit_unauthorized`, `submission_submit_denied`, `submission_submit_blocked_missing_evidence`, and `submission_submitted`, 6 states, 6 routes, 10 records, 5 permission scopes, 6 guardrails, and 6 acceptance checks. Final readback found 50 text nodes, zero suspicious clipped text nodes, and zero child overflow. `get_design_context` and `get_screenshot` succeeded; screenshot returned `1024x1013` from original `1360x1345`.
- 2026-05-21 05:54 PT: Figma node `190:2` (`Prototype / 29 / Submission detail access audit handoff`) was created after `GET /api/submissions/:id` gained missing-session, denied, scoped readback, actor-role-scope, and no-storage-identifier proof. Shared plugin data key `senior_capstone/submission_detail_access_audit_contract_2026_05_21` records consumed run `2026-05-21-0535-submission-detail-access-audit-mvp-012`, consumed implementation commit `48de622de03b1c1ea660ee8b613d6b23c3d139b4`, consumed closeout commit `427d9fd17d6493765fa853c9bb4289810c6c71fa`, routes `GET /api/submissions/:id`, `/student/submissions/:id`, `/workspace`, `/api/submissions/:id/evidence`, `/api/submissions/:id/submit`, `/admin/audit`, and `/api/audit-events`, audit events `submission_detail_unauthorized`, `submission_detail_denied`, and `submission_detail_viewed`, 6 states, 7 routes, 12 records, 7 permission scopes, and 6 guardrails. Final readback found 51 text nodes, zero collapsed text nodes, zero suspicious clipped text nodes, and zero child overflow after layout correction. `get_design_context` and `get_screenshot` succeeded; screenshot returned `1009x1024` from original `1360x1381`.
- 2026-05-21 06:16 PT: Figma node `196:2` (`Prototype / 30 / Protected record audit acceptance board`) was created as a verification/handoff board after no newer non-Figma proof existed beyond node `190:2`. Shared plugin data key `senior_capstone/protected_record_acceptance_matrix_2026_05_21` records `ready_for_rebuild_followup`, consumed nodes `173:2`, `178:2`, `180:2`, `183:2`, `187:2`, and `190:2`, 7 consumed endpoint families, 6 remaining proof families, 7 role scopes, and 8 acceptance checks. Final readback found 122 text nodes, zero suspicious clipped text nodes, zero child overflow, consumed-node references present, and the remaining proof queue present after row-width/autosizing/text-height correction. `get_design_context` and `get_screenshot` succeeded; screenshot returned `512x1024` from original `1360x2724`.

## Current Highest-Risk Requirements

1. `MVP-032`, `MVP-033`, and `MVP-041` through `MVP-045`: the canonical role-aware workspace now has D1-backed admin, program-teacher, and mentor dashboard routes/UI, primary-color role styling, local auth fallback, and tenant/Google Workspace SSO schema/route scaffolding. It still needs hosted proof after deploy, production Google OAuth client/env configuration, Bryan's tenant-domain and auto-provisioning decisions, and a live section-level permission-denied state.
2. `MVP-036` and `MVP-037`: the public website now has the Student Guide / Teacher Guide top-banner mode, but still needs fuller no-hidden-core-content coverage proof across every public route.
3. `MVP-006`, `MVP-012`, `MVP-014`, and `MVP-017`: permission/protected-record tests now cover the student dashboard route, the teacher review queue empty-scope regression, review history access audits, review decision unauthorized/denied audits, evidence link/check/upload/download paths, submission detail readback audits, submission submit unauthorized/denied/blocked/success audits, mentor meeting read/mutation access audits, and no-storage-ID evidence summaries; Figma nodes `173:2`, `178:2`, `180:2`, `183:2`, `187:2`, and `190:2` have consumed the dashboard, teacher review queue, review history/decision, evidence-link, submission-submit, and submission-detail route-level audit proofs, and node `196:2` now consolidates those proofs into a rebuild acceptance matrix. Remaining workflow endpoints and browser/live permission-denied states still need the same depth before real student records.
4. `MVP-013`: Drive upload/download code plus workspace upload/download UI actions exist, live runtime Drive credential parts are configured, Shared Drive root `0AJHkstxfN-dTUk9PVA` is wired through local config, Cloudflare Pages production/preview vars, and remote D1, and `npm run check:drive:live` now passes hosted fake `.test` upload/download, a >5MB resumable upload/download, D1 metadata/audit verification, denial guards, dashboard download URL redaction, and storage-ID leak checks. Remaining proof is browser-level upload progress/retry handling and Google Docs export cases.
5. `MVP-009`: source-framework seed loader and local D1 verification exist; remote D1 apply/verification is pending on `CLOUDFLARE_API_TOKEN`.
6. `MVP-015` through `MVP-018` and `MVP-022`: alpha demonstrates workflow actions and several production endpoints exist; the teacher review queue now audits access and default-denies empty program/cohort scopes; review history, review decision, mentor meeting, presentation slot, and archive/export endpoints now audit more unauthorized/denied/viewed/transition states with redacted role scopes where applicable; the canonical workspace now surfaces presentation slot status/check-out/check-in controls and a student Archive tab backed by persisted archive-readiness rows, while archive manifests now include provider-unavailable audit handling, scoped expired-download behavior, retention-policy state, and Drive-backed package-file upload logic locally. Outline approval still needs a durable gate beyond slot status if required, and hosted presentation/archive UI proof remains open.
7. `MVP-004`: account lifecycle remains incomplete after first-admin bootstrap, but reset-required password completion now exists in `/api/auth/complete-reset`, admin reset initiation now exists in `/api/admin/users/:id/require-password-reset`, active-user self credential rotation now exists in `/api/auth/change-password`, and admin import now exists in `/api/admin/users/import` with generated reset-required temporary credentials for fake `.test` proof, denied-role audit, reset-first login proof, redacted audit coverage, and a default block on real non-`.test` admin-visible temporary credential imports unless `ALLOW_REAL_TEMP_CREDENTIAL_IMPORT=true` is explicitly set after policy acceptance. The canonical workspace now includes an admin-only import UI with source/VM coverage, hosted marker proof, memory-only setup output clearing, non-admin import gating, and local credential-backed Pages/D1 import/reset proof after consuming Figma node `158:2` and node `163:2` without direct Figma work. Remaining follow-up: Bryan must decide the real-user credential delivery policy, and rebuild should continue hosted permission-state proof plus broader role-scope/protected-record hardening.
8. `MVP-026`: Cloudflare Pages/D1 live proof now passes with the user-scope scoped token; each new post-push deployment still needs hosted route/API/asset verification, and Drive live proof now passes fake upload/download, >5MB resumable proof, metadata/audit, denial, and leak checks against the Shared Drive root.

## Laddering Rule

The Functionality UX Upgrades must:

1. Pick the highest-risk incomplete requirement in the lane's target area.
2. Prefer implementation, tests, deployment evidence, a verified Figma handoff, or an exact blocker over planning.
3. Update this catalog only when a requirement's status, evidence, blocker, or next acceptance check materially changes.
4. Write the structured run manifest with the requirement IDs touched.
5. Commit and push every repo change, or commit a blocker record that names the exact unavailable account/tool/policy step.
